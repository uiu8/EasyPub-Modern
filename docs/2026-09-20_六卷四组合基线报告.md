# 六卷四组合基线报告

日期：2026-09-20。任务 P0-1；代码为当前未提交工作区，不是已发布版本。

## 结论

四组合已执行，均未获得完整验收。没有丢失样本中现存的独立正文，但参考目录默认建议存在跨卷误配；不能将“运行成功”解释成“修复正确”。

## 方法与复现

入口：`tools/repair-baseline/repair-baseline.csproj`。在仓库根目录执行：

```powershell
& 'D:\software\dotnet-sdk-10.0.302\dotnet.exe' run --project tools/repair-baseline/repair-baseline.csproj -c Release -- .
& 'D:\software\dotnet-sdk-10.0.302\dotnet.exe' run --project tools/repair-baseline/repair-baseline.csproj -c Release -- . --plain-catalog
```

每个组合只编辑独立临时副本，隔离设置、目录库和备份。运行前核对样本文件哈希及每份正文哈希，结束核对缺陷样书未变。使用默认识别配置、默认勾选建议，不授权源文删除，不执行人工移动或分割，不访问网络。

完整真值 690 章；缺文 11 章；独立现存正文 679 份；含副本的正文实例 690 份；真实跨卷移动 21 章。正文由独立真值的三行 SHA256 识别，不依赖程序识别出的章号。

## 两轮输入必须区分

原目录含 `# 第1卷 旅程1` 等 Markdown 前缀，当前导入器不将其识别为卷。原始运行保留在 [原目录结果](../work/baseline/20260920-123721-ff9ed8/baseline.json)。它混合了目录格式和后续对齐问题，不能单凭这轮判断对齐算法。

第二轮仅在临时目录输入中移除 `# `，断言得到 **6 卷、690 章**，没有修改样本文件或产品导入器。结果：[标准卷标题结果](../work/baseline/20260920-123842-49fc98/baseline.json)。每组保存输入目录、建议、决定、最终树和结果文件。

## 标准卷标题运行结果

| 组合 | 默认选中 | 最终树条目 | 独立正文/实例 | 标题归属疑点 | 顺序倒退点 | TXT 改变 |
| --- | ---: | ---: | --- | ---: | ---: | --- |
| M1 本地/树 | 0 | 668 | 679/690 | 29 | 1 | 否 |
| M2 目录/树 | 143 | 685 | 679/690 | 142 | 6 | 否 |
| M3 本地/写回 | 0 | 668 | 679/690 | 29 | 1 | 否 |
| M4 目录/写回 | 143 | 685 | 679/690 | 142 | 6 | 是 |

四组合未发现现存正文缺失或越界行，均仍含 11 种重复正文。M4 执行 131 次标题替换，没有删除或插入行。685 是树条目数，包含层级节点，不等于已恢复章节数。

“标题归属疑点”采用标题完全相等比较，包括缺“第”等纯格式差异，不全是串章。“顺序倒退点”是相邻独立正文在真值顺序中的下降点，不是移动章节总数。

但已确认具体串章：M2 中 `v5-c30` 正文被归到“第30章 卷4的旅程30”，源标题行 2296；31～37 章同样跨卷误配。标准目录输入仍可复现，不能归因于 Markdown 前缀。

原目录运行的 M2/M4 默认选中 196 项、最终条目 679、归属疑点 195、顺序倒退点 11；与第二轮分开留档，不混用数字。

## 缺陷分派

2026-09-20 后续复测：B01已修，证据 `work/baseline/20260920-172924-4a8600/baseline.json`，MIX-M1零变化返回false且消息为“章节树与原文均无变化”。下表保留初始观察；B02～B04未因本次状态修复而解决。

| 编号 | 已观察事实 | 后续归属 |
| --- | --- | --- |
| B01 | M1 零选择、零变化仍返回 Changed=true，提示“已更新” | P1-2 统一执行状态 |
| B02 | Markdown 卷标题被作为章节输入 | P2 输入合同：明确支持或提示，不能悄悄当普通章 |
| B03 | 有效六卷目录下仍跨卷误配并生成默认建议 | P3-1/P3-3 卷作用域、缺标题与缺文的保守对齐 |
| B04 | 本地默认选择为空，混合缺陷未修复 | P3 各场景核对检测、建议及人工路径，不能据此宣称功能不存在 |

## 限制与下一步

这是默认自动建议的核心基线，未覆盖 UI 人工选择、保存重开、成品输出、读者设备和全部 36 格。记录的阶段耗时包含审计及首次运行开销，只作诊断，不作为性能基准。

下一步仍为 **P0-1：独立场景基线**，将混合失败拆为可定位输入，补反例和预期合同；随后 **P0-2：当前代码全量测试快照**。B03 修复前不能按这批默认建议交付自动修复版本。
