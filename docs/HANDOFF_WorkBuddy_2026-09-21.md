# WorkBuddy 接手入口 · 2026-09-21

> **2026-09-24最新入口：P3-1功能验收完成，下一步P3-2。** 请先读[完成报告](2026-09-24_P3-1完成报告.md)及实施记录顶部。最新已交付包为v1.62.0 Kindling，本次后续修改尚未打包；下文版本/进度均为历史。

## 先确认现场

- 唯一工作目录：`D:\software\DeepSeek-Harness\workspace\easypub-branch`。原仓库 `C:\Users\13168\Documents\Codex\2026-08-22\easypub` 不得修改；不要 push。
- 本次接手时 HEAD `d6b2a3e`，main 分支，有大量跨批次已修改及未跟踪文件。必须先查看 git status/log，保留全部现场。只拉取 GitHub 不会得到这些开发改动。
- 已发布基线 v1.61.2；重构尚未打包发布。旧 HANDOVER 下文中的 HEAD、测试数量都是历史事实。
- 用户现在明确要求把交接写入 Memorix，供 WorkBuddy 继续。连接后把项目绑定到本工作目录，项目应为 `uiu8/EasyPub-Modern`；先检索“WorkBuddy 章节重构交接”。

## 阅读顺序与产品约束

1. 本文。
2. `2026-09-19_章节编辑与修复完整开发方案.md`，尤其末尾整体计划。
3. `2026-09-19_章节重构实施记录.md`，最新记录在顶部；需要细节再读历史批次。
4. `章节重构验收台账.md`、六卷及独立场景基线报告。

原始用户 TXT 不原地修改，操作经 Mutate 或既有副本事务路径；删除必须备份及逐行清单。缺失正文诚实报告，不编造标题/正文。四组合是有/无参考目录 × 仅成品章节树/修订文本副本。UI 必须说明依据、落地范围、按钮后果、预览和撤销。每批都写实施记录及下一步，不能零散改动而不更新整体计划。

视觉方向已确认：简洁黄纸油墨，全局一致，可有克制水墨点缀；用户希望未来按钮用渐变水墨。视觉实施后必须让用户截图确认。近期批次未改视觉，不代表设计已全部落地。无 Kindle 真机，不能宣称设备验收。

## 当前阶段与最近完成

当前 **P3-1 实施中，未完成**。P1 会话/执行安全、P2 规则快照/输出一致性已有证据，仍有未验收项，详见整体计划。

- `ChapterBreakpoints`：中数/阿拉伯数字混排按体系比较，相同父级跨体系共享已出现号码；避免假缺口与重复报缺。未动 ChapterDiagnostics 识别层。
- `ReferenceLocator`：先精确锚点，再邻接区间弱匹配；目录中的精确身份不能被另一卷的模糊章节抢走。精确标题建索引，47 份自树目录对照定位单轮 12199→1229 ms，结果不变；不是外部目录成功率实测。
- `SelfRepairPlanner`：空白不算正文；A/A/B 重复组不会因为 A/A 一对相同就把全组当可删除。按锚点比较降低组内比较次数；完整副本无目录时仍保守不默认推荐。
- `ReferencePlan`：连续同名空标题链保留正文承载节点，保护手工节点及分别被目录占用的同名节点。仅覆盖已识别的独立树节点，不处理吸收到正文里的重复标题行。
- **本批 `ChapterContentSplit`**：按 ContentRanges 的阅读顺序切分，而非按原文行号大小重分组；选中行留在新章正文。`ChapterEditorWindow.Split_Click` 和按钮条件共用合同，仍由 Mutate 撤销。9 个新增测试覆盖倒序/不连续范围、边界与逐行顺序守恒。没有新增标题输入或文本副本插入引导。

## 验证与证据

- 本批测试结果见实施记录顶部；测试文件 `work/test-results/2026-09-21/p3-manual-split-full.trx`、`p3-manual-split-desktop.trx`。桌面 RepairApplyWorkflow 是已有应用流程回归，不等于本次切分按钮的真实点击/撤销已验收。
- 上一批 Core 901/901，证据 `work/test-results/2026-09-20/p3-reference-empty-full.trx`。
- 最新六卷四组合报告 `work/baseline/20260920-225117-af56e4`：全部保留 679 份存在的正文；有目录两模式顺序倒退 0、正文归属异常剩 11（第四卷30–40无标题待人工切分）；无目录两模式归属异常29、顺序倒退1。第四卷50–60的11份正文真正不存在，不能补造。
- 47份实书位于桌面 novel/so-novel（40）+ tomato（7）。旧文档44本已过时。自树对照目录不等于真实参考目录真值；空标题规则实书探针0命中仅说明未误触发，其有效性证据来自构造夹具。
- `work/diagnostics/p3-reference/`、`p3-duplicates/`、`p3-reference-empty/` 存有探针结果。

## 下一步：沿同一场景闭环

1. 已补人工切分的WPF按钮、撤销/重做及项目存储器保存重开合同（2/2）；下一步做§6.5标题输入和前后正文预览，不要重复补同一测试。
2. 缺标题正文的操作明确区分“新增成品标题，保留选中正文”和“采用现有标题行”；修订文本副本模式需预览插入、行号映射、事务回退。不能把本批纯范围修复当完整人工切分流程。
3. 六卷混合样本按真值辅助选定11个缺标题边界，验证人工完成后正文连接不变、归属问题消除；真正缺章仍留报告。再推进 P3-2。重复建议的区间合并与完整12格验收仍欠缺。
4. UI 落地、截图确认与最后打包按开发方案执行。不要自动发布或推送。

## 构建与测试坑

SDK 不在 PATH，使用 `D:\software\dotnet-sdk-10.0.302\dotnet.exe`。修改 Core 后，先将其 obj/bin 移到 `work/build-backups/<时间>-<批次>/` 并生成文件清单，校验路径在支线内；避免 CoreCompile 静默跳过造成旧DLL。确认输出 `EasyPub.Core -> ...dll`。

```powershell
Set-Location 'D:\software\DeepSeek-Harness\workspace\easypub-branch'
$dotnet='D:\software\dotnet-sdk-10.0.302\dotnet.exe'
& $dotnet test tests/EasyPub.Core.Tests -c Release -- xUnit.MaxParallelThreads=1
& $dotnet test tests/EasyPub.Desktop.Tests -c Release --filter FullyQualifiedName~RepairApplyWorkflowTests -- xUnit.MaxParallelThreads=1
& $dotnet run --project tools/repair-baseline -c Release -- . --plain-catalog
```

基线工具第一个参数必须是根目录 `.`。桌面 WPF 全量混跑曾有宿主线程/备份目录竞态，隔离目标组结果不能冒充全量通过。Core 有既有400ms性能阈值边界波动；失败如实记录，不偷改预期。新的识别启发式需跑全部语料，本批切分是确定性范围操作，无新增识别启发式。

## Memorix 回执

已通过本机 CLI 写入并重新检索验证：项目 uiu8/EasyPub-Modern，记忆 #862，标题「WorkBuddy 章节重构交接 2026-09-21」。插件 store 曾报 graph_entities.projectId 非空约束错误；CLI 可用，不需要因此重装或清空记忆。


## 后续增量：切分交互合同

2026-09-21新增 ChapterBatchTests.Manual_split_buttons_undo_redo_and_project_reopen_preserve_reading_order，两例通过；证据 work/test-results/2026-09-21/p3-manual-split-ui-final.trx。测试通过真实按钮事件和项目存储器，非主窗口保存按钮全流程；没有新的产品逻辑或视觉改动。最新实施记录顶部优先于本文之前的待验收描述。

## 最新增量：标题与边界预览

ChapterSplitWindow已接入切分按钮：必填标题、前后各8行预览、取消无修改，确认才Mutate。明确仅改成品树，选中行保留正文。2/2交互合同与1/1实际WPF渲染通过，证据p3-split-preview-final.trx及p3-split-preview-capture-final.trx；截图work/test-results/2026-09-21/split-preview-light.png待用户确认。下一步副本插入预览、规则校验、坐标映射、事务回退；目录选标题和六卷人工验收仍缺。此前关于标题输入未实现的描述已被本节更新。


## 最新续接：2026-09-23 副本切分已收尾

此前被Kindling评估中断的ManualSplitCopy已编译并验证。Core916/916，切分桌面3/3，WPF渲染1/1；证据work/test-results/2026-09-23/。新增可选独立TXT+章节项目，原稿不变，失败版本备份后恢复副本，外部变化不覆盖。导出项目仅带当前书章节与默认转换配置，不克隆全部制作设置。当前树可撤销，导出副本保留，视觉待用户确认。下一步六卷11处人工边界真值验收；阅读最新实施记录顶部，记忆#912的副本代码未验证描述已被本节取代。Kindling仍仅评估，未接入。Memorix增量#930。

## 最新：六卷人工切分两路径通过
2026-09-23报告docs/2026-09-23_六卷人工切分验收.md及work/baseline/20260923-084949-340613/。M2树/M4连续副本各11个真值辅助边界，679份正文各一次、第四卷89章、归属与顺序错误0，真正缺失11章不造。修复ManualSplitCopy的末尾换行空行映射，Core917/917。下一步P3-1剩余分类反例和区间提示，不能当自动识别或完整用户/成品/设备验收。


## 2026-09-23 v1.62.0 交付更新

人工切分副本路径与重复分类本批反例收尾已交付，新增Kindling可选引擎（兼容MOBI，默认仍KindleGen）。详见[使用说明](v1.62.0-使用说明.md)及实施记录顶部。Core定向25/25，桌面设置和主窗口配置往返各1/1；成品启动通过，便携ZIP与Setup位于outputs。用户要求简单测试，本次不主张全量回归或设备通过。截图work/test-results/2026-09-23/kindling-settings.png待用户确认。

下一步：先接收本版UI与实际书稿反馈；继续P3-1区间提示整合、目录选标题与剩余全模式验收。P3总体未完成。Kindling AZW3、专用元数据/完整设备兼容暂未开放。最新用户已授权打包，未授权本次push；保留全部未提交支线现场。

### 2026-09-23 后续：重复区段分析索引优化

用户确认v1.62.0的Kindling速度满意；已说明该包包括此前当前支线的已完成修改。继续P3-1：ChapterReviewAnalyzer对诊断行号、标题行及同父同级同名建立一次索引，编号提醒合并使用一次集合过滤，减少每组重复扫描。检测证据与处理入口不变，定向33/33通过（review-index.trx），没有新启发式和视觉变动，本批未打包。下一步是区段汇总→逐对比较的导航与去重展示，必须保留选择保留哪一份的操作，不能直接丢掉单对提醒。总体重构仍未完成。

### 2026-09-23 重复区段逐对核对入口

工作台区段提醒新增“逐对核对本区段…”：选择标题+两处行号对应的一对，直接打开既有正文对比/保留选择，不批量删除，不更改原稿。RepeatedSequenceWindow按区段交替A/B节点ID重验正文，仅呈现仍有效的实际配对，不依赖当前200组分页提醒。定位原文入口保留。桌面定向1/1通过，截图work/test-results/2026-09-23/重复区段逐对核对.png待视觉确认；未打包。下一步是折叠区段内单对提醒及过滤、加载更多、修改后刷新合同；本轮并未隐藏现有提醒，也未完成整个P3。

### 2026-09-23 区段提醒折叠完成

Desktop ChapterReviewPresentation在分页前将实际区段配对的同名正文重复提醒折入汇总，保留全部证据/节点/定位，逐对选择入口仍可用；不改检测及删除算法。例中5项变1组。定向4/4，包括真实工作台、成员搜索、200项分页及加载更多、处理后树刷新退回独立对。截图重复区段折叠工作台.png待确认。尚未打包，v1.62.0包不含本批后续改动。下一步人工切分时从参考目录选择标题及现行规则识别检查，整体P3仍未完成。

### 2026-09-23 人工切分参考目录标题入口完成

切分对话框接工作台SavedReference，显示来源和带卷名的章标题，支持筛选及明确“填入标题”；不默认选中、不自动覆盖手输、不推断正文身份与边界。无目录沿用手输；副本走既有ManualSplitCopy完整规则验证。定向1/1，split-catalog-final.trx；截图人工切分选择目录标题.png待用户确认。未打包，本批未宣称完整四模式验收。下一步汇总P3-1剩余验收格，串起目录选题→副本导出→项目重开证据。

### 2026-09-23 · P3-1 当前阶段与下一步

本步完成目录选标题→树切分/副本导出→保存重开联通合同，5/5通过（work/test-results/2026-09-23/p3-1-catalog-split-chain.trx）。新增两条有目录路线，与原三条本地路线共同覆盖取消、明确填入、非单调正文范围、撤销/重做、项目存储器重开；副本的正文逐行顺序不变。目录只是标题参考，未自动认定正文身份。未改产品代码/未打包，用户现有v1.62.0包不含包后新增UI。

用户要求每步明确P编号：后续进度和记录均以“P3-1（或实际阶段）· 本步动作/验证/下一步”表述。P3-1仍实施中，S5关联证据不等同S1/S2/S4的12格收尾。下一步P3-1回归这三类的完整处理→保存重开→成品正文证据；原P3-3人工切分相关底层证据保留，不提前标阶段完成。

### 2026-09-23 · P3-1 / E10 重复处理到EPUB成品

DuplicateOutputRoundtripTests四例4/4：完整副本保留前/后，空标题本地/目录依据，均经项目保存重开与真实EPUB逐章标题/p正文校验；原稿SHA256不变，两段正文各一次且顺序正确。证据work/test-results/2026-09-23/p3-1-duplicate-output.trx，原稿/项目/EPUB保留在p3-1-duplicate-output子目录。E10仅补S2本地树及S4本地/目录树局部证据，不把M3/M4写回、原始识别形状、设备等未测范围标完成。无产品改动/无新包。P3-1下一步：S1卷名丢失分卷后的保存重开与成品目录/正文合同。
