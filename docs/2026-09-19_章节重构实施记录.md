# 章节重构实施记录

依据：[完整开发方案](2026-09-19_章节编辑与修复完整开发方案.md)。基线 `d6b2a3e` / v1.61.2。

## 当前进度

### 2026-09-24 · P5-3 后续：v1.63.1 打包与 GitHub 发布准备

- 用户确认章节工作台是独立弹窗后，移除重复主流程左栏；保留三栏内容、顶部依据/范围、底部撤销/重做与应用返回。主窗口 XAML 和配色未改动。
- 版本字段统一为 **1.63.1**，代号 `popup-shell-fix-kindling`。发布脚本改为按版本选择使用说明；旧 v1.63.0 包继续保留。
- 产物：`outputs/EasyPubModern-v1.63.1-popup-shell-fix-kindling-win-x64.zip`（74,376,062 字节），`outputs/EasyPubModern-Setup-v1.63.1-x64.exe`（71,536,333 字节）。SHA-256 见 `RELEASE_v1.63.1.md`。
- 验证：前一轮 Desktop 定向 11/11、主窗口 63/63、截图 1/1；此次 ZIP 解压后 6 个文件哈希与发布目录一致，EXE/Setup 版本均为 1.63.1.0。解压目录独立启动显示 `EasyPub Modern v1.63.1`，正常退出码 0，用户配置/恢复/备份目录使用隔离路径。
- 包内 Kindling 烟测 **4/4**：实际 TXT 与 EPUB 两种输入，EPUB 保留版式/兼容重排，共 3 条真实转换路径；同时验证旧设置默认引擎兼容。核对 BOOKMOBI 头、日志、成品大小和源哈希。证据位于 `work/release-v1.63.1/packaged-kindling.trx` 与 `package-verification.json`。
- 用户本轮明确授权发布 GitHub，覆盖此前“不推送”的发布限制。已核对 `origin` 为 `uiu8/EasyPub-Modern`、远端 main 与开发基线一致；GitHub 最新公开版为 v1.61.2，无 Actions 工作流。此次提交包含此前本地章节修复及 Kindling 实现，生成日志和 Python 缓存留在本地。
- **下一步：**提交源码及说明，上传 ZIP/Setup 并核对远端 SHA-256、字节数、标签提交和正式发布状态；然后进入《整体优化计划》P1。Kindle 真机仍待设备。

### 2026-09-24 · P5-3 完成：Kindling 集成发布包

- 版本闸门已统一为 **v1.63.0**：桌面程序集、发布目录、ZIP、Setup 的版本字段一致；发布目录可独立启动，包内保留完整使用说明和 Kindling 许可文件。
- 便携目录：`outputs/EasyPubModern-v1.63.0-p5-complete-kindling-win-x64/`；ZIP：`outputs/EasyPubModern-v1.63.0-p5-complete-kindling-win-x64.zip`（74,376,334 bytes）；Setup：`outputs/EasyPubModern-Setup-v1.63.0-x64.exe`（71,533,756 bytes）。
- Kindling 随包路径为 `bin/kindling-cli-windows.exe`，源文件与包内SHA-256一致；包内专项烟测4/4通过，P5-1全链20/20和36格证据均统一使用Kindling转换出口，不以KindleGen结果替代。
- 便携程序已独立启动烟测通过，未发现残留进程；包内 `使用说明.md` 明确了四种修复组合、Kindling设置、原稿保护、限制与Kindle真机待验收项。
- 回归：Core 功能回归（排除独立长文本性能项）976/976 通过；长文本 8000章性能测试独立运行1/1通过（预检查295ms，低于400ms预算）。全量测试时该项受其他测试并发/内存负载影响曾测到976ms，未修改阈值或掩盖回归，保留独立基准证据：`tests/EasyPub.Core.Tests/TestResults/p5-long-isolated-2.trx`。
- 本次没有推送远端；Kindle真机因当前无设备仍列为待验收，不把目录、封面、字体和翻页行为写成已验证。

**P5-3 结论：**开发方案 §13.5 的发布退出条件已满足，当前版本可交付；下一步转为收集实际使用反馈和补做Kindle真机验收，不再继续扩展本轮范围。

### 2026-09-24 · P5-2 完成：47 个真实语料只读正确性与性能基线

- 实际语料不是历史文档中的44本：本机 `so-novel` 40个、`tomato` 7个，共 **47个物理TXT**；按SHA-256去重后为 **42种唯一内容**，其中5组是重复文件副本。
- 新增只读工具 `tools/corpus-baseline/`，逐文件执行生产章节识别、`ChapterReviewAnalyzer`、`ChapterBreakpoints` 和 `ChapterTreeDocument.ValidatePlan`；每个文件做冷加载与同进程热加载两轮，记录章节/卷/提示/断点/重复标题跳过数、分阶段耗时、分配字节和工作集变化。
- 运行结果：47/47成功，47/47源文件SHA-256不变；冷加载中位数45.3ms、平均61.5ms、最大255.8ms；热加载中位数44.5ms、平均57.2ms、最大252.8ms；本地诊断中位数35.8ms、最大679.2ms；断点计算中位数0.5ms、最大18.3ms。未发现异常超时或树合法性失败。
- 报告：`work/corpus-baseline/20260924-131555-925/results.json`、`summary.md`。这轮没有联网、没有写回TXT；转换出口的实成品验证统一使用P5-1的Kindling证据。
- 结论：当前识别/诊断的热点集中在大文件和重复提示较多的书（如《一世之尊》《赤心巡天》《全球高武》），暂不引入未经全语料验证的新启发式；性能基线作为后续优化对照。
- **下一步：**进入P5-3，统一版本号、构建、便携ZIP、Setup、Kindling随包和启动烟测，最后更新交接文档。

### 2026-09-24 · P5-1 完成：36 格四组合全链验收（Kindling）

- S1～S4共20格既有全链证据，加S5/S6/S7/S8/S9共16格，本轮合计 **36格**；每格均覆盖当前书稿/参考目录 × 仅整理章节树/同时修改TXT，执行保存重开、正文指纹/顺序/归属、原稿保护和成品出口。
- 所有转换出口统一使用 `C:\Users\13168\Desktop\kindling-cli-windows.exe`，并断言输出BOOKMOBI头、文件大小、日志存在；Kindling输出的`ChapterCount`包含卷/前置目录项，因此验收同时断言保存树中的正文章数（S5=690、S6=679）和转换计划总条目数，不把计数口径差异误报为内容丢失。
- S5标题缺失但正文在：11个边界必须人工指定，未凭空建章；S6标题与正文都缺失：11个`MissingBody`不执行、不造空正文。S7缺“第”、S8纯数字标题、S9编号错字均只改标题候选，不做正文全局替换。
- 证据：`work/test-results/2026-09-24/p51-s1-s4-kindling-final.trx`、`p51-s3-kindling.trx`、`p51-kindling-final.trx`、`p51-candidates-kindling.trx`；P51专项共20格，Kindling与结构断言均通过。
- **下一步：**P5-2真实语料基线已完成，进入P5-3发布构建；Kindle真机仍因无设备单列待验收。

### 2026-09-24 · P5-1 启动：四组合全链验收台账（历史记录）

- P4-2 已按用户最终反馈收口：章节工作台保留三栏与底部提交栏；全局配色恢复原版；导入、检查、制作设置和转换验收页面不做架构重排。
- 桌面 Release 构建 0 警告、0 错误；工作台定向回归通过：`WorkbenchContextBarTests` 5/5、`WorkbenchForgetCatalogTests` 2/2、`OneClickRepairAffordanceTests` 1/1、`ControlStyleContractTests` 6/6、`RepairBasisVisualTests` 1/1。
- 真实工作台截图已生成并复核：`work/ui-screenshots/00-章节工作台.png`。截图中当前书稿/参考目录、仅整理章节树/同时修改 TXT、三栏、撤销/重做和“应用并返回”均可见。截图测试退出时仍触发既有 STA 宿主异常，但 PNG 已写出，产品构建与定向测试不受影响。
- 本轮 P5-1 先建立四组合矩阵和 S1～S9 证据台账，不把已有单元/专项通过误报成完整全链通过；Kindle 真机因无设备继续列待验收。
- P5-1 第一轮隔离基线已完成：S5/S6 各 M1～M4 均无运行错误，报告分别为 `work/baseline/20260924-124524-799b6f` 与 `work/baseline/20260924-124526-fcb506`。S5 四格保留 690 份正文但留下 11 个正文归属待人工边界，未自动建章；S6 四格保留 679 份独立正文且没有把缺失正文伪造回来；所有本轮源副本字节未变。该证据只说明分类与保护边界，尚不等于 UI、保存重开或成品验收。
- **下一步：**逐格执行“导入/检查 → 预览 → 应用或取消 → 保存重开 → 转换预检”，优先覆盖 S1 卷边界、S2 完整重复、S3 章节移位和 S5/S6 缺标题/真缺文。

### 2026-09-24 · P4-2：真实工作台视觉与可用性（完成，用户已确认）

- 工作台落地为真实可用的黄纸油墨布局：左侧制作流程导航，右侧章节修复工作区；保留现有树、问题列表、原文核对、撤销/重做和“应用并返回”操作。
- 顶部新增固定三项操作说明：依据、应用范围、可以撤回。它们由当前工作台状态计算，不把“参考目录”和“写回 TXT”伪装成同一个切换器。
- 当前真实含义：本地/参考目录由“本次仅按本地检查”和目录状态决定；只改树/写回 TXT由修复落地设置决定；另存修订副本单独说明不覆盖原稿。
- 视觉验收截图：work/test-results/2026-09-24/p4-2-screenshots/26-工作台-下一步做什么.png；小屏 27-工作台-小屏布局.png。小屏 1080×680 下主按钮、取消、工作区标题和状态提示均可见。
- WPF 定向渲染 6/6通过，随后小屏渲染 1/1通过；证据 p4-2-visual-final.trx、p4-2-context-final.trx、p4-2-final-layout-small.trx。没有进行真实高 DPI 设备点击验收。
- **下一步：**进入 P5-1 全链验收。当前未打包、未 push。

### 2026-09-24 · P4-1：工作台问题卡、模式提示与按钮引导（完成）

- 问题卡继续复用 IssueResolutionPolicy 的同一决策源，按钮文案、可用性和后果说明不再由界面事件自行猜测。
- 工作台顶部用“下一步”摘要说明本地可修、需要目录、必须人工核对三类数量；新操作说明卡固定回答“依据什么、会改哪里、如何撤回”。
- 原文外部变化、识别规则变化、无待核对和目录缺失均显示下一步，不再让问题卡静默消失；“应用并返回”仍是唯一提交入口。
- P4-1 视觉结构没有改变修复事务，所有按钮继续走已有预览、备份、撤销/重做和失败恢复链。

### 2026-09-24 · P3-4：缺“第”、数字标题、编号错字（功能完成）

- 保持三类问题分流：缺“第”或单位的标题进入 MissingChapterHeadings 候选；纯数字标题进入 ChapterEditingDocument 的数字标题候选；编号错字使用每本书的 HeadingNumberCorrections，不做正文全局替换。
- 修复一个实际安全门缺口：编号纠错候选（例如“第一百一十久章”）即使与前一章正文相邻，也可进入候选；缺前缀候选仍要求隔离行，避免把普通叙述误切成章节。错字候选仍经过编号范围、长度、标点和单字符映射检查。
- S7/S8/S9 保护合同已加入专项验收：缺“第”不等于缺正文；001：标题能生成规范化候选；正文“天长地久”不被替换；S9 默认路径给出“第119章”候选并保留原正文。
- 验证：P3-3/P3-4专项 5/5，Core 全量 957/957；证据 tests/EasyPub.Core.Tests/TestResults/p3-3-4-acceptance-codefix-final.trx、p3-3-4-core-final.trx。Core bin/obj 已在 work/build-backups/p33-p34-core-20260924-105626 留存清单。
- 本阶段没有视觉主题改动、没有打包、没有 push；完整四模式矩阵和真实窗口视觉仍属于后续 P4/P5 验收。

### 2026-09-24 · P3-3：缺标题与真缺文、人工分割/插标题（功能完成）

- S5 仅有正文时不自动把正文第一句升级为标题，也不凭目录制造空章；用户必须在工作台指定边界后使用既有“插入标题/人工分割”路径。
- S6 标题和正文都缺失时只报告 MissingBody，不可勾选、不可推荐、不借用后卷同号正文；这样“无正文”不会被误报为可修复章节。
- 人工插标题先在预览中用当前识别规则重新识别，再通过 ManualSplitCopy 写入独立修订副本；保留原稿字节、备份、项目和插入身份，保存重开后行号映射仍成立。
- 验证：S5/S6分类、人工切分副本、正文保留与保存重开共 5/5通过，和既有工作台切分按钮合同合并作为 P3-3 证据；未把目录定位当成正文存在证据。
- **下一步：**进入 P4-1 工作台问题卡、模式提示和按钮字典；P3-3/P3-4功能链已完成，完整 36 格混合样本归入 P5-1。

### 2026-09-24 · P3-2：工作台另存修订副本收尾（阶段完成）

- 移动预览现在有两个明确按钮：`确认移动（仅成品）` 与 `另存修订副本…`。前者通过Mutate改变当前树，可撤销/重做；后者不把副本的新行号套回原稿树，成功后给出副本TXT、项目、movement.json清单及备份路径。
- 另存路径在后台重新校验原稿哈希，调用ChapterRelocationCopy，副本事务完成前当前树不改变；窗口关闭后也不会把WPF控件更新投递到已关闭窗口。导出失败/外部改稿/中断均保留原稿并走既有恢复，不显示伪成功。
- 修复物理移动边界缺陷：选章正文范围不包含标题行，之前简单样本可过而真实连续区段会被错误拒绝；现将选中标题行纳入连续区段校验并映射。
- 验证：物理移动Core 8/8（本/目录基准各一例，前后移动、混合换行、EOF、21章六卷、外改拒绝、非连续/共享行拒绝、三中断点）；按钮导出1/1，`work/test-results/2026-09-24/p3-2-physical-final.trx`、`p3-2-export-ui.trx`。导出按钮真实生成TXT/项目/清单并确认当前树不变；原稿字节保留。
- P3-2四格目前：M1树批量移动、M2目录树移动、M3本地修订副本、M4目录修订副本均有Core/按钮证据；M3/M4尚未加入完整EPUB/Kindle读者验收，设备验收不在本阶段。P3-2阶段功能链完成，下一阶段P3-3。
- 无视觉主题改动；移动预览截图仍需用户确认。未打包/push。


### 2026-09-24 · P3-2：修订副本物理移动与中断恢复（Core本步完成）

- 新增ChapterRelocationCopy：复用显式选章/目标的树预览，仅接受物理连续、完整、无其他章节共用行的区段；移动清单核验全部源行恰好出现一次。对非连续正文、缺原文标题行、目标在区段内/紧邻其后明确拒绝，不猜边界。
- 单次构造原行→新行映射；重映射全部标题行和正文区间，保留人工阅读顺序，Canonical模型核对移动前后正文。副本和.easypubproj一起导出；工作副本之外的原稿不写入。
- 将ManualSplitCopy的已有事务导出/失败恢复抽为内部PreparedTextCopy，切分和移动共用。未改事务执行规则；移动以逐行替换承载最终排列，配独立movement.json记录真实移动区段、目标、源/目标哈希和每行来源，不能把底层替换误称删除章节。
- 写入策略：保持编码、BOM、物理行槽位的换行符及EOF换行状态，仅排列文本。这意味着混合换行符不会随原行搬动，清单明确注明；不是承诺每个移动行的终止符原样跟随。
- 必须先保存移动清单，再执行有备份及执行manifest的副本事务；导出前再次验原稿哈希。失败时沿用既有恢复机制，仅恢复自己的新副本版本，保留失败产物。
- 验证17/17：新移动测试7例 + 原切分副本7例 + 前批树移动3例；work/test-results/2026-09-24/p3-2-physical.trx。移动包括前/后方向、混合换行与有/无末尾换行、外部改稿拒绝、非连续/共享源拒绝；BackupCreated/SourceReplaced/DocumentReloaded三中断点均原稿不变、副本恢复、无成功项目残留。
- S3六卷21章：实际TXT从头重新识别以及导出的项目重开，均六卷690章，逐章正文哈希/标题/卷归属正确，旧位置不留副本；原稿及修改前备份字节一致。产物在work/p3-2-acceptance/physical-*。这不是M3/M4完整界面或EPUB/Kindle验收。
- Core bin/obj备份及清单在work/build-backups/p32-*，确认重新编译。本步未改视觉、未打包/push；前一批移动预览截图仍待用户确认。
- **下一步仍P3-2：**把“另存修订副本”接入移动预览，显示实际移动行段和目标，失败不应用当前树，成功给项目/备份/清单路径；补真实导出按钮及取消/撤销边界，再收齐S3四格。


### 2026-09-24 · P3-2：工作台批量移动预览与撤销（本步完成）

- 入口：Ctrl/Shift选择连续章节 → 专项处理 → 选中章节 → 移动到指定章节之前（仅成品，预览）。窗口显示选区起止标题、搜索目标卷/章、卷路径和原文行号；必须显式选目标，不自动选首项；已位于目标前时不执行空移动。
- ChapterRelocationWindow调用上一批Core预览，确认后通过Mutate重建树并保留选章；确认前重新比对原文SHA256。原文变化则停止，不套用旧坐标。整卷、非连续、失效ID等由Core拒绝，反馈显示在预览中。
- 为什么独立标“仅成品”：物理TXT区段移动尚未实现，此入口不受写回偏好误导，也不把树排序冒充TXT移动。移动后可撤销/重做，不删正文、不生成重复章节项。
- 验证：ChapterBatchTests.Relocation_menu_preview_cancel_apply_undo_redo_preserves_source实际点击菜单，检查未选目标/搜索不自动确认、卷路径提示、取消、两章跨卷应用、撤销、重做及源文本不变，1/1通过；work/test-results/2026-09-24/p3-2-move-ui.trx。初次捕获程序化窗口NameScope未创建的问题，已修复。前批Core15项是历史证据，本次未重复全量测试。
- 正式App主题截图：work/test-results/2026-09-24/章节移动预览.png，已查看无遮挡；**待用户视觉确认**，不默认通过P4。未打包/push。
- **下一步 P3-2：修订副本的物理移动**，设计显式移动区段及逐行来源映射，保证21章归位后旧位置无副本；备份/清单/保存重开/失败恢复齐全，再联通实际文本路径。P3-2仍实施中。


### 2026-09-24 · P3-2：真实移位的树模式两路径（本步完成，阶段仍实施中）

- 核对现状：参考目录整理已有树排序；原文补丁只有插入/替换/删除，无实际移动操作。现有工作台上/下移为单章同级操作，不能视作批量跨卷入口。
- 新增 ChapterTreeRelocation.Before：显式指定连续同层级叶章节和目标章，生成预览结果，按目标层级移动整组，保留正文区间/身份，标记人工编辑；拒绝空选、失效ID、选区内目标、分散选择、整卷或前置内容，验证覆盖守恒。当前仅Core逻辑，尚未接工作台新按钮。
- 新增 ChapterRelocationAcceptanceTests；复用原12格验收合同。有目录S3-M2走既有真实修复服务，无目录S3-M1模拟用户明确选中卷2第20～40章并指定第41章之前，绝不由同号猜目标。
- 结果：新增3项 + P3-1十二格共15/15通过，work/test-results/2026-09-24/p3-2-tree.trx。两条S3链路6卷690章，全部正文哈希/顺序/卷归属与真值一致，项目保存重开和EPUB输出一致，原TXT字节不变。产物work/p3-2-acceptance/S3-M1-*、S3-M2-*，accepted.json表示该Core链路通过，不表示完整UI格已验收。
- 构建前Core bin/obj已备份至work/build-backups/p32-*并生成清单，确认重新编译Core。本步无视觉改动、未打包/push；没有宣称新的Core全量测试数。
- **下一步仍P3-2：**接入工作台“连续选章→明确目标→移动预览→Mutate应用→撤销/重做”，再实现修订副本实际文本移动、逐行来源映射/移动清单及失败恢复。S3-M3/M4不能因树排序成功而标为通过。


### 2026-09-24 · P3-1：卷边界与重复分类收尾（阶段完成）

- 完成 S1/S2/S4 × M1～M4 共12格功能链验收：每格保存项目并重开、输出实际 EPUB；六卷690份独立正文、顺序/归属/哈希一致。修订副本删除备份、逐行清单与事务校验齐全，原稿不变。
- 修复人工首卷重开被当成前言；本地重复清理层级被拉平；目录中现有卷标题误列为并回正文；目录卷节点重复生成；源版本迁移丢失无正文父卷及识别配置。
- Core最终941/941通过。桌面最终混跑21通过/1图标资源加载失败，失败项独立进程1/1通过，完整保留两份TRX；这是既有WPF测试隔离限制，不称合并批次全绿。
- 核心最终验证与具体证据见 [P3-1完成报告](2026-09-24_P3-1完成报告.md)。桌面实际应用/撤销12项、复核/备份恢复10项通过；47本重复检查非空正文误删0。
- 该阶段不包含P4视觉确认或P5全部场景；未打包/push，v1.62.0不是本次开发结果。
- **下一步 P3-2：真实章节移位**，先处理S3六卷样本21章归位的预览、树排序和修订副本移动合同；绝不拿删除副本替代移动。

### 2026-09-23 · P3-1：重复处理后项目重开与EPUB正文验收（本步完成）

- 目标：补S2完整副本/S4空标题重复处理后的持久化及实际成品证据，不新增识别或删除启发式。
- 新增DuplicateOutputRoundtripTests四例：完整副本明确保留前/后两种选择；空标题按本地/参考目录两种依据处理。每例保存.easypubproj→存储器加载→按保存树重新加载原稿→实际生成EPUB→逐章检查标题与p正文数组。转换时故意给不匹配的全局规则，确保使用保存树。
- 结果：4/4通过，work/test-results/2026-09-23/p3-1-duplicate-output.trx。实际原稿/项目/成品保留在同目录p3-1-duplicate-output子目录。正文两段各一次、顺序正确、无冗余标题混入正文，原稿SHA256不变；空标题删除范围仅标题与空白。
- 范围：仅树修复后的项目和EPUB成品合同；使用显式含重复节点的识别结果，不覆盖原解析器吞并标题的另一种形状。不代表所有写回模式、真实点击或Kindle已验收。未改产品代码、未改Core实现、未打包/push。
- P3-1下一步：补S1卷名全失的有/无目录分卷→保存重开→成品目录与正文顺序合同，然后复核12格剩余缺口。P3-1总体继续实施，不提前进入P3-2。

### 2026-09-23 · P3-1：目录选标题到副本重开联通验收（本步完成）

- 当前阶段明确为P3-1卷边界与重复分类；人工切分同时为P3-3提供前置证据，不提前宣称P3-3完成。用户新增要求：每次说明当前P几，后续进度/记录/下一步均标阶段。
- 本步把上一批单窗口选择接入已有真实切分按钮合同，增加有目录只树与有目录副本两条路线，使用隔离目录存储中的跨卷同名标题，确认必须点击填入才覆盖手输。
- 延续非单调正文范围场景；校验取消不修改、不生成副本，确认/撤销/重做/项目保存重开；副本项目真实加载后比较逐行正文阅读顺序，不只检查标题字符串。
- P3-1验证结果：5/5通过，work/test-results/2026-09-23/p3-1-catalog-split-chain.trx。原三条树/副本路线与新增有目录树/副本路线均通过。副本项目加载后按保存树读取正文，顺序与切分前一致；取消无产物，当前树撤销/重做及保存重开保持位置。源文件不变，未改产品代码、未打包/push。此证据是WPF路由按钮及存储器重开，不冒充物理鼠标、成品或真机验收。
- P3-1下一步：回到S1卷名全失、S2完整副本、S4空标题重复的12格收尾清单，优先补重复处理后的保存重开/成品正文证据；本步只补S5人工切分关联证据，不把P3-1或36格全标完成。

### 2026-09-23 续：人工切分选择参考目录标题（入口完成，视觉待确认）

- 用户要求继续并更新记录。本批在已有人工切分窗口增加可选目录标题选择，使用工作台当前保存的参考目录，不联网获取、不自动猜边界。
- 显示目录来源、卷名和标题，按卷名/标题筛选；初始不选中，必须点“填入标题”才替换输入框，可继续手改。目录标题不等于该正文身份已确认，不写reference验证标记。无目录保留手输流程。
- 副本导出仍交ManualSplitCopy按完整当前规则验证；仅树路径允许自定义标题，原稿不变。
- 验证：ChapterSplitCatalogTests定向1/1通过，覆盖同名跨卷、排除卷节点、按卷筛选、选择不覆盖手输、明确填入、空筛选禁用、边界及原稿不变；报告work/test-results/2026-09-23/split-catalog-final.trx。真实主题截图暴露ComboBox选中项使用ToString显示对象调试字符串，已覆盖为卷名+标题并补断言；最终截图人工切分选择目录标题.png。本批未重跑完整导出事务，复用既有规则验证链，不宣称本次完整四模式验收。
- 下一步：汇总P3-1剩余12格用户路径证据，优先串起选目录标题→另存副本→重开项目验证；不再只追加零散入口。仅Desktop改动、未打包/push，当前可运行包仍v1.62.0。

### 2026-09-23 续：重复区段提醒折叠（已完成本批，视觉待确认）

- **原因与效果：**区段汇总与每对提醒同时出现在待核对列表，造成重复导航。本批新增Desktop ChapterReviewPresentation展示投影，只将区段明确A/B配对的chapter_content_duplicate折入汇总。示例4对+1汇总由5项变1组；汇总保留全部NodeIds、原文位置、RelatedIssues，既有逐对选择入口继续可用。
- **边界：**不改Core诊断/修复方案/正文，不隐藏缺章、跨标题重复或区段成员之间并非原配对的提醒。汇总覆盖的正文证据没有删除。分页之前折叠，显示计数和继续加载使用同一投影。区段不足3对时，重新诊断后剩余独立对自然显示。
- **验证：**4/4定向通过（work/test-results/2026-09-23/review-folding-ui.trx）：保留无关对及缺章、证据不变、200项分页边界/加载更多、无汇总时独立对保留；真实工作台显示一个汇总并保留按钮，搜索成员标题保留汇总，模拟移除两份后的刷新恢复两条单对提醒。原稿不变。刷新测试直接提供处理后的树，不冒充真实删除/撤销端到端验收。
- **截图：**work/test-results/2026-09-23/重复区段折叠工作台.png，用户视觉确认待完成。仅桌面修改，未打包/push，当前交付仍是v1.62.0。
- **下一步：**人工切分窗口接入参考目录标题选择，区分候选依据与当前识别规则可识别性；继续P3-1剩余用户路径，整体重构未完成。

### 2026-09-23 续：重复区段直接进入逐对比较（入口完成，视觉待确认）

- 原因：区段提醒只有定位入口，用户必须回到全书问题列表找单对提醒。本批增加“逐对核对本区段…”选择列表，列明标题、双方原文行号及相似情况，再进入已有正文对比与Mutate处理流程。
- 边界：不会批量删除，不预选保留哪份；原文定位入口保留。只读取区段实际配对ID，打开前重新验证当前正文，失效/已移除成员不再显示；不从区段成员交叉配对，也不依赖最多200组的已加载提醒。
- 验证：RepeatedSequenceWindowTests定向1/1通过，覆盖4对顺序、成员被移除/正文变化后排除、选择第三对返回同一对象、空列表禁用、原稿不变以及真实WPF控件渲染；报告work/test-results/2026-09-23/repeated-sequence-picker-final.trx。首次截图测试未加载应用资源，修正测试宿主后重渲染；最终截图“重复区段逐对核对.png”。这不是物理鼠标端到端删除验收。仅Desktop改动，未改Core识别，未打包/push。
- 下一步：用户确认视觉；完成区段汇总与单对提醒的折叠展示、过滤/加载更多与处理后刷新合同。当前仍保留单对提醒，不能宣称重复展示已消除。

### 2026-09-23 续：重复区段分析索引优化（已完成本批）

- 用户已确认Kindling转换速度满意，询问发布范围：v1.62.0来自当前整个支线，包含之前已完成修改；未完成事项不视为完成。该反馈不等于所有UI视觉确认或设备验收。
- 核对发现区段摘要与逐对对比承担不同操作，直接隐藏逐对提醒会丢失保留选择入口。本批先优化生成过程，不改变识别、分组范围、删除权限或默认建议。
- 为诊断行号、章节标题行、父级+层级+同名建立一次索引；编号提醒整合改为一次移除集合，避免逐组扫描全列表。
- 验证：Core定向33/33通过，覆盖ChapterReviewAnalyzer、正文重复、目录验证抑制、样本提示及样本回归；报告work/test-results/2026-09-23/review-index.trx。Core备份bin/obj及CSV后确实重编译。构建仅有NuGet审计源暂不可达及既有分析器警告，测试成功。未新增检测启发式，不扩大自动删除范围，不宣称全书耗时提升倍率。没有UI改动、没有重新打包，用户现有v1.62.0包保持原样。下一步接区段摘要到逐对处理的UI导航，保留逐对选择能力后再减少重复展示。

### 2026-09-23 完成：v1.62.0 重复反例收尾与 Kindling 可选引擎交付

- **完成本批：**新增短诗/相同短模板/正文提及标题3个反例及完整副本明确选择后正文仅保留一次的回归；既有规则通过，无新增删除启发式。此前人工切分副本/事务/末尾换行修复一并纳入本包。区间提示整合、目录选择标题等后续 P3 事项未完成，不将整个重构标绿。
- **用户入口：**制作设置 → MOBI / Kindle → Kindle 转换引擎 → Kindling（实验性 · 兼容 MOBI）→ 应用设置。输出选MOBI；路径留空使用随包0.45.6。KindleGen保持默认，EPUB不变。未开放AZW3。
- **实现：**MobiOptions.Engine/KindlingPath随项目及方案持久化；MainWindow保留完整MobiOptions后再覆盖旧控件字段，修复新字段在主窗口收集配置时丢失的问题。独立KindlingWriter，TXT当前树→EPUB，EPUB保留与重排均接入；读取双流防死锁、取消终止进程、检查退出码与BOOKMOBI头、成功才替换成品，独立日志保留。不走KindleGen后处理、不静默回退。
- **兼容决策：**初次3条试转均被KDP无封面规则拦截；个人本地转换使用--no-validate，仍保留应用预检和Kindling正文自检；UI/日志/说明均声明。只复用移除附加源文件，其余KindleGen专用控件置灰。引擎缺失参与预检及缓存指纹。
- **验证：**Core定向25/25（含真实Kindling TXT/EPUB保留/重排3条、重复分类及副本切分）；Desktop设置应用/切换/渲染1/1，主窗口方案往返1/1。最初截图宿主资源问题通过测试宿主Style=null解决，未改产品样式。报告kindling-release-smoke-final.trx、kindling-settings-pass.trx、kindling-main-profile.trx均在work/test-results/2026-09-23。
- **交付：**outputs/EasyPubModern-v1.62.0-kindling-win-x64，exe67.80MiB；同名zip70.91MiB；EasyPubModern-Setup-v1.62.0-x64.exe68.22MiB。ZIP清单核实主程序/config/两引擎/许可。成品启动InputIdle=true、Responding=true，标题v1.62.0；探测包内Kindling0.45.6。启动检查隔离设置与恢复路径。原构建归档work/release-backups，Core旧产物备份附CSV；未push。截图kindling-settings.png供用户确认。
- **限制与下一步：**按用户要求未做全量回归/47本再测，没有新检测启发式；未真机验收。先收集本版UI和真实书稿转换反馈；之后继续P3-1区间提示与目录选标题，再考虑独立AZW3出口。完整重构尚未完成。

### 2026-09-23 续：重复反例收尾与 Kindling 可选引擎（实施中）

- 用户新范围：完成中断任务后接入 Kindling 并打包可运行版，只做必要冒烟验证。
- 先补短诗/短模板/正文提及标题/完整副本选择后删除反例；不提高自动删除范围，不引入新启发式。
- Kindling 通过独立适配器输出兼容 MOBI，复用已有 TXT 章节树到 EPUB、EPUB保留与重排两条路径。默认仍为 KindleGen；独立日志、取消、成功后替换输出，失败不静默回退。
- 设置明确禁用不支持的 KindleGen 专用选项；保留移除附加源文件选项。首版未开放 AZW3。
- 下一步：定向测试、发布编译、运行包启动与截图、便携包/安装包、更新结果及 Memorix。不宣称全部章节重构完成。

### 2026-09-23 续：P3-1 六卷11处人工边界验收（两条路径通过）

- **目标：**用独立真值提供第四卷30–40的11个边界，分别验证树切分与连续另存副本/重开后的逐章正文哈希、顺序和归属。真值仅用于验收工具，绝不加入产品自动识别算法。
- **退出条件：**有目录整理后的679份现存正文各出现一次，归属与顺序正确；第四卷89个有正文章节，50–60缺文不建空章；保存重开保持结构，原始样本哈希不变。UI物理点击与设备验收不在本工具证据范围。
- **实际结果：**M2仅树与M4连续另存副本/重开各完成11次切分，第四卷89个有正文章节，全书679个独立正文且仅出现679次；归属错误/顺序倒退/丢失正文均0。50–60真缺文未建空节点，原样本与每次切分输入文件哈希不变。报告 `work/baseline/20260923-084949-340613/`；解释见 `2026-09-23_六卷人工切分验收.md`。
- **修复：**真实六卷暴露ManualSplitCopy未映射末尾换行产生的编辑器空行位置，导致副本导出被拒绝。现明确映射旧/新末尾空行；新增末尾CRLF回归，与无末尾换行共同保留字节。工具改为实际逐行内容比较，不误用章内位置哈希；保存重开的六个卷HeadingLevel默认值0→1按有效层级核对，未改产品识别。
- **验证：**Core全量917/917（work/test-results/2026-09-23/p3-six-volume-manual-core.trx），基线工具两模式退出0，git diff --check通过。Core已备份产物后重建。真值是测试输入，不是自动补章算法；无视觉改动，不冒充物理点击、EPUB/MOBI成品或Kindle验收。
- **下一步：**返回P3-1剩余验收格，补完整副本/空标题重复/同号异正文反例与区间提示。目录选标题与全模式完整用户路径仍未完，不宣布P3整体完成；不打包、不push。实施记录、总计划、台账及Memorix同步。
### 2026-09-23 续：P3-1 人工切分另存副本路径完成

- **目标与范围：**接续中断的 ManualSplitCopy，不接入 Kindling。本步把切分窗口的可选“同时另存含新标题的TXT副本与章节项目”接到实际事务。默认不勾选；原稿永不作为事务替换目标。
- **用户操作：**选择正文行→填写标题并核对边界→勾选另存副本→确认。后台先按当前完整识别配置检查插入标题能否成为独立节点，再写独立副本；不能识别、规则尚未应用或原稿版本变化则拒绝。成功后显示配套 .easypubproj 路径，打开该项目继续才能保留已有手工移动/合并。当前树由Mutate切分，撤销只还原当前树，已导出副本保留；窗口已提前说明。
- **正文/位置合同：**只在所选行前插入一个标题，选中行仍属新章正文；复用SourcePatchRenderer/SourceCoordinateMap，逐章比较规范化正文模型，保留不连续和非递增范围。副本保留编码/BOM/逐行换行，保存完整映射后的章节树。不能把仅重新识别TXT视为保存树重开。
- **执行与恢复：**独立副本经SourceEditTransaction、修改前备份和执行清单写入。写入后后续失败时，仅在副本仍匹配本次预期哈希、备份匹配原稿哈希时自动恢复；恢复前保存失败版本，已生成的新项目一并归档。外部修改不覆盖。目录/权限导致恢复也失败时如实报告副本及备份位置。没有删除正文。
- **代码修复：**后台任务前在UI线程读取标题，避免后台访问TextBox；桌面合同补界面同步上下文，初次宿主中止不计通过，仅给本测试设置上下文，未全局改变其他测试的等待策略。
- **实际验证：**Core新增6例：真实副本、UTF8 BOM+CRLF+无结尾换行、非递增正文顺序和项目重开、严格规则拒绝、准备后原稿变化，以及BackupCreated/SourceReplaced/DocumentReloaded三处中断恢复。Core全量 **916/916**，桌面真实按钮含取消/确认/副本/撤销重做/重开 **3/3**，实际WPF窗口渲染并导出 **1/1**。证据 work/test-results/2026-09-23/p3-split-copy-core-full.trx、p3-split-copy-ui-final.trx、p3-split-copy-capture.trx；截图 split-copy-preview.png。Core产物已备份清理重编译，git diff --check通过。
- **边界：**导出是当前书的章节项目，使用默认转换配置，不冒充原项目全部元数据/封面/制作设置的克隆。当前窗口仍编辑原稿对应树；不会静默切换书稿。用户视觉确认待完成。容器节点暂拒绝副本切分，目录选标题仍缺，六卷11处完整人工边界未验收；P3-1/P3-3不标完成。无Kindle，未打包/发布/push。
- **下一步：**按六卷样本独立真值确认第四卷30–40的11个切分点，验证现存679份正文不丢不重，并保留50–60真实缺文报告；随后回到P3-1剩余验收格。避免重复开发本次副本合同。
### 2026-09-21 续：P3-1 人工切分标题与边界预览（代码完成，视觉待确认）

- **目标：**从现有切分按钮打开确认窗口，填写非空新章标题、核对前章末尾与新章开头，确认才 Mutate；取消不改变树。选中行保留为正文，本次明确只改成品章节树。
- **性能边界：**只读取前后各8行，每行预览最多500字符，不拼接整章正文；显示完整正文行数及预览范围。
- **退出条件：**确认/取消/空标题合同通过，撤销重做及保存重开仍通过，提供实际WPF窗口截图供用户确认；文本副本插入路径不在本步伪装完成。
- **实际结果：**新增 ChapterSplitWindow 并接入现有按钮；标题为空或控制字符时不允许确认，取消不改树，确认使用去首尾空白的标题。前后各8行、每行最多500字符的局部预览保留原文行号及阅读顺序。桌面目标合同2/2通过（p3-split-preview-final.trx），真实WPF窗口渲染与完整交互1/1通过（p3-split-preview-capture-final.trx），位于 work/test-results/2026-09-21；截图 split-preview-light.png。Core未改未重跑。
- **限制与唯一下一步：**用户尚未确认截图；本次仅树操作，尚无目录选标题和文本副本插入。下一步接入明确的副本插入预览/规则验证/坐标映射与事务回退，再完成六卷11处边界验收。无Kindle、未打包发布。
### 2026-09-21 续：P3-1 切分按钮、撤销重做与项目重开验收

- **本步完成：**新增2条真实WPF工作台按钮回归，以移动/合并后非递增正文范围为初始状态，分别在后一范围首行与范围内部切分。检查首个正文行不能切分、所选行仍在新章正文、节点身份、人工标记及正文逐行顺序。
- **完整链条：**导航选中原文→触发现有切分按钮→撤销→重做→项目存储器保存/读取→按保存方案加载源文档→重新打开工作台。验证各章正文范围及标题不变，原TXT内容不变。保存用真实项目存储器；未模拟主窗口“保存项目”按钮，不冒充完整鼠标端到端验收。
- **验证结果：**新增桌面目标组 **2/2**，证据 `work/test-results/2026-09-21/p3-manual-split-ui-final.trx`。初次失败是测试重开时直接传入原识别文档，漏掉主窗口使用的 existingPlan 加载步骤；修正测试装置后通过，无产品逻辑变更。Core未改，本步未重复全量；上一批910/910记录仍有效。未改视觉或打包。
- **下一步：**开发方案§6.5的手工分割交互：输入/选用新章标题、预览前后两章、确认后才切分；明确与“采用已有标题行”的区别。修订文本副本模式需接入插入预览、行号映射与事务撤销。随后沿六卷样本完成11个缺标题边界的人工验收；P3-1仍在实施中。

### 2026-09-21 WorkBuddy 接手确认：P3-1 切分交互闭环待验证

- **当前任务与本次边界：**接续 P3-1 卷边界与重复分类，优先完成六卷混合场景暴露的人工切分用户路径；相关缺标题能力关联 P3-3，不据此宣布 P3-1 或 P3-3 完成。本轮仅阅读、核对现场与更新记录，不改生产代码，不运行窗口或重跑测试，不打包、发布、提交或推送。
- **接手核对已完成：**已读最新交接入口、完整开发方案（重点 §13.5）、本记录最新批次、验收台账、六卷及独立场景历史基线。git status/log 确认唯一工作支线 `D:\software\DeepSeek-Harness\workspace\easypub-branch`，main / HEAD `d6b2a3e`，大量既有修改及未跟踪文件保留；不修改原仓库。
- **证据核对已完成：**直接核对既有 `work/test-results/2026-09-21/p3-manual-split-full.trx` 与 `p3-manual-split-desktop.trx` 的计数，分别为 910/910、11/11，失败为0。这是上一批运行结果核验，不是本轮新测试；桌面11例仍不证明切分按钮、撤销/重做和保存重开已验收。
- **下一步唯一主任务：**在隔离副本和设置下，先检查 `ChapterEditorWindow.Split_Click`、Mutate、撤销/重做及保存重开入口，补经过真实移动/合并形成非单调、不连续 ContentRanges 的桌面操作合同。逐步核对按钮条件、所选正文行保留、切分前后正文串联逐行一致、无重叠遗漏、撤销/重做还原、重开后标题/范围/顺序一致及源 TXT 哈希不变；按钮路由事件测试与物理点击走查分开记录。
- **后续顺序与退出边界：**上述合同通过后，完善“新增成品标题并保留选中正文”与“采用现有标题行”的区分；修订文本副本路径补插入预览、规则验证、行号映射和事务回退。再按独立真值人工确认第四卷30–40的11个缺标题边界，验证现存679份正文不丢不重，第四卷50–60真正缺失的11章只留报告、不补造。返回 P3-1 对应12格与重复建议区间收敛，再推进 P3-2。
- **持续记录约定：**每个子步骤开始记录编号、目标和退出条件；完成或遇阻立即写实际结果、证据、未验收项与唯一下一步，同步总体计划及受影响验收格，不等到整轮结束才补记。视觉变化需真实截图由用户确认；无 Kindle 不宣称设备验收。
- **记忆同步已完成：**已将 Memorix 会话绑定 `uiu8/EasyPub-Modern`，成功检索交接记忆 #862；本轮接手确认已写入 **#867《WorkBuddy 接手确认：P3-1 切分交互验收与实时实施记录》**，再次按标题检索成功。同步校准总方案旧的 Memorix 禁止条款和验收台账过期的下一步，保留历史测试/基线事实与未验收状态。
- **文档检查：**`git diff --check` 返回0，仅提示既有文件 LF/CRLF 转换警告；此命令不覆盖未跟踪文档，不能作为新文档内容验收或程序测试。

### 2026-09-21 续：P3-1 人工切分保留阅读顺序与 WorkBuddy 交接

- **完成内容：**新增 Core `ChapterContentSplit`，按有序正文范围切分；修正移动/合并后原文行号非递增时，旧逻辑按行号大小错分正文的问题。按钮可用性与执行共用同一合同，首个正文行和范围外行不可切分，后续范围首行可以切分。
- **用户操作与后果：**仍在章节正文中选择一行、使用现有拆分按钮。选中行保留为新章节第一行正文，新章标题仍为“新章节”，不吞掉正文作为标题。操作继续走 Mutate，可撤销，原始TXT不变。本步没有视觉改动。
- **验证：**新增9例验证非递增/不连续范围、边界、逐行顺序守恒及输入不变。Core全量 **910/910**，桌面已有 RepairApplyWorkflow **11/11**，证据 `work/test-results/2026-09-21/p3-manual-split-full.trx`、`p3-manual-split-desktop.trx`。Core产物已备份并重建，Desktop已编译。桌面11例不代表切分按钮真实点击和撤销已验收。
- **交接：**新增 `HANDOFF_WorkBuddy_2026-09-21.md`，记录工作区未提交现场、产品约束、构建坑、证据边界和接手顺序。按用户本轮明确授权通过 CLI 写入 Memorix 项目记忆 #862，并重新检索成功；插件写入曾报 projectId 约束错误。换工具后应接续本地现场，不从旧发布版本重做。
- **下一步：**补人工切分的真实桌面操作、撤销/重做和保存重开合同；再明确新增成品标题与采用已有标题行的区别，以及修订文本副本插入预览。沿六卷样本完成11处缺标题边界验收，真正缺正文仍保留报告。P3-1仍未完成，未打包/发布。

### 2026-09-20 续：P3-1 参考目录连续空标题的保留锚点

- **完成内容：**参考目录已定位到连续同名标题的首份空节点时，现在沿中间仅有空白的重复标题链，保留末份正文承载节点。只有输入树确实含这些节点、前份正文全为空白、均为非手工叶节点，且后一份没有分配给另一目录条目，才调整锚点。
- **建议与操作：**仍从“按目录检查并生成建议”进入；空标题清理默认推荐，说明明确列出保留行、仅移除标题/间隔空白、不移除正文。整章副本继续按正文一致性判断默认建议；TXT 删除继续走既有明确选择。没有新增按钮或调整界面布局。
- **保护边界：**手工章节不被换锚点；目录两卷各列同名标题时，两份保持各自归属。未识别成独立树节点的重复标题行不在本步自动清理范围，不能拿本功能替代原文行清理；不同正文也不由空标题规则处理。
- **验证：**新增连续三标题、手工保护、跨卷同名目录三例，验证保留节点ID、移除逐行清单和两段正文完整保留。Core全量 **901/901**（`work/test-results/2026-09-20/p3-reference-empty-full.trx`）。47份语料以当前树标题去重构造对照目录，模拟本次空标题动作，0个命中、0行移除、0非空正文删除（`work/diagnostics/p3-reference-empty/corpus.json`）；这是无新增误触发检查，效果证据来自明确夹具，不冒充真实目录验收。六卷四模式报告 `work/baseline/20260920-225117-af56e4`，各模式679份正文均保留；有目录仍为0处顺序倒退、11处待人工切分。`git diff --check`通过。
- **下一步：**进入缺标题正文人工切分的路径检查：明确选中原文哪一行、插入标题还是将现有行作为标题、正文如何完整保留，以及两种落地模式的预览与撤销。优先锁定正文范围合同，再接入界面引导；视觉改动仍需用户截图确认。P3整体尚未完成，本步不发布新版本。

### 2026-09-20 续：P3-1 自修复重复证据边界

- **完成内容：**自修复现在按原文是否含非空白字符判断一章是否有正文，空行、空格、全角空格和制表符不再把空标题节点伪装成有正文的章节。恰好一份有正文时，沿既有结构证据保留该份，默认建议移除空标题节点。
- **修复风险点：**旧 `AssessIdentity` 找到任意一对相同正文便认定整组 Confirmed；但移除动作覆盖整组，A/A/B 三份同名章会把不同正文 B 一起列入动作。现在每份都必须与保留锚点有正文证据；任一份不同即不产生整组移除动作。比较次数由全组配对上限 O(n²) 收敛为对锚点 O(n)。本步不实现 A/A/B 中单独选 A/A 的子组动作，仍可通过正文对比核对。
- **用户操作与后果：**“基于当前章节树检查”可默认选中有唯一正文归属的空标题清理；完整正文重复、无法决定保留哪份时仍需用户核对；同号不同标题/正文不生成重复移除动作。只改树仍只影响成品；源文件删除仍需既有显式选择与事务备份。无新增按钮、无视觉修改。
- **验证：**新增5例覆盖无空行、一空行、含全角空格/制表符、A/A/B混合正文、同号不同标题正文，并验证应用空标题清理后两章正文都保留。Core **898/898** 通过，证据 `work/test-results/2026-09-20/p3-duplicate-final.trx`。相邻标题可被默认识别规则合并，因此测试显式构造保留两个标题的合法输入树，覆盖用户自定义规则场景。
- **语料检查：**47份TXT自修复生成741条重复动作，其中2条默认推荐；实际模拟应用仅排除4行标题/空白，非空正文删除数为0，总自修复与模拟应用耗时1212ms（一次观测）。报告 `work/diagnostics/p3-duplicates/corpus.json`；没有修改语料。六卷四模式再次执行，报告 `work/baseline/20260920-224622-62069c`，完整性与参考目录路径待核对边界保持。
- **下一步：**重复分类尚未全部退出验收。有目录路径还需统一“连续重复标题保留有正文的那份”的锚点选择，并校准空标题与整章副本的建议文案；随后做缺标题正文的人工切分引导。无目录整章副本不默认删除的既定证据规则继续保留。当前不打包、不发布，不把898项测试当作整个章节编辑完成。

### 2026-09-20 续：P3-1 参考目录跨卷误配与精确索引

- **本步完成：**修复缺失章节先占用后卷同号标题、随后引发连续错配的问题。参考目录的精确标题候选先确定归属；所有模糊候选延后到精确定位完成后，仅在前后已定位章节之间搜索。后续兜底也不得占用另一个目录条目的精确标题。精确匹配的错位章节仍可定位，不因物理位置错乱而丢弃。
- **用户操作与结果：**沿现有“按目录检查并生成建议 → 核对 → 应用”操作即可。只有确实定位到的标题才能用于整理；缺口保持待核对。只改树模式保留源文件，改文本模式使用既有事务与备份路径。本步没有增加按钮或修改视觉。
- **六卷真值验收：**`work/baseline/20260920-223550-90524b` 四模式完整运行。在有目录的两种落地模式下，679 份现存正文全部保留，成品中正文出现数从 690 降至 679（11 份完整副本被排除），顺序倒退从 6 处降至 0；正文归属异常从 142 处降至 11。剩余 11 处全部是第四卷 30–40 章标题完全缺失、正文仍由第29章承载，不能自动猜边界。第四卷 50–60 章标题和正文均缺失，不补造。新增真值测试逐一核对 668 个已定位标题与正文身份，22 个无标题章节保持未定位。无目录模式本步未改变。
- **旧测试纠正：**旧 480 章样书的固定预期包含 62 次错误 Retitle。逐条检查后，正确 Retitle 为 7 次且全部只是补“第”；新断言同时验证这个具体变化，不只改数量。建议数 191→129、AddChapter 10→8、MissingBody 41→43。模糊标题在精确后章之外的回归用例改为保留未定位，明确需要人工判断。
- **性能：**精确标题按键一次建索引，已经精确命中的章节不再对全书候选逐个算相似度。47 份真实语料用当前识别树生成定位对照目录，索引前后 Found/Missing/错配数完全一致：30107 个定位、11 个未定位、0 个错误标题键。合计定位耗时 12199→1229 ms（同机单轮观测，非稳定基准）；未定位项是对照探针中的候选限制，不能解释为真实缺章。报告 `work/diagnostics/p3-reference/corpus-before-index.json`、`corpus.json`。此对照验证精确定位与性能，不等于47本外部参考目录修复的人工验收。
- **验证：**Core 全量 **893/893**（`work/test-results/2026-09-20/p3-reference-final.trx`）；桌面实际应用流程 **11/11**（`p3-reference-desktop.trx`）；`git diff --check` 通过。Core 构建产物已按批移入 `work/build-backups/` 并生成文件清单；六卷基线工具验证样书哈希未改变。未打包或发布新版本。
- **下一步：**继续 P3-1，先把“连续重复标题行、完整正文副本、同号不同正文”的建议和默认选择分别锁定，再处理第四卷缺标题正文的人工切分引导。顺序异常报告仍可能逐章重复提示、部分说明文字沿用旧行为，应合并区间并校准文案；不能据本步结果宣称整个章节编辑或P3完成。

### 2026-09-20 续：P3-1 混用编号体系的断点降噪与索引优化

- **目标与退出条件：**在不改 `ChapterDiagnostics` 识别层的前提下，处理同一父级/层级里中文数字与阿拉伯数字混排造成的假断点；同时不能把另一种写法里确实存在的章号再次报成缺章，并保持真实同体系缺号仍可逐章提示。
- 断点比较现在使用两层作用域：相邻编号比较按“父级、层级、章/回单位、编号书写体系”隔离，所以 `第十七章 → 第335章` 不会被解释成中间数百章缺失；`present` 与范围仍按不含书写体系的父级/层级/单位共享，避免 `第257章` 与邻近中文数字标题互相抵消或重复报缺。编号纠错字符也归入中文数字体系，保持书内纠错表语义一致。
- 将重号集合与宽松读数改为一次索引，避免每个候选号码重新扫描整棵树；结果锚点、缺号阈值、目录缺失逻辑和正文均未改写。
- 新增 3 条回归：中数→阿拉伯数切换不报跨体系缺口；切回中文后仍检查同体系真实缺号；另一写法已经出现的号码不重复列为缺章。`ChapterBreakpointsTests` **13/13** 通过。
- **全量验证：**Core 串行 **888/889**，唯一失败是既有 `LongTextPerformanceTests` 预检预算边界（本次全量 404 ms，目标 400 ms）；同一性能项单独运行 **1/1** 通过。相关证据：`work/test-results/2026-09-20/p3-numbering-core-full-final.trx`、`p3-numbering-performance-final.trx`。桌面目标组 **11/12**，唯一失败是 `SampleBookRepairFlowTests` 的 WPF 测试宿主线程访问竞态，非断点逻辑；断点标签与工作台上下文其余 11 项通过，证据 `p3-breakpoint-desktop-targeted.trx`。
- **全语料探针：**当前两个语料目录实际有 40+7 个 TXT，共 **47** 个文件；当前断点统计为 1071 处（具名缺号 1037、不可逐号编号差异 34）。这是断点探针计数，不等同工作台全部问题组，也不是缺章真值。加入共享 `present` 后，探针总数由本批前的 1076 降到 1071；《择天记》断点由 28 降到 27。报告：`work/diagnostics/p3-numbering-family-report.json`。
- **限制：**这轮只收敛混用编号的断点作用域和性能热点，没有改卷归属、跨卷移动、完整正文重复判定或 `ChapterDiagnostics`；47 个文件中重复书稿仍按文件统计。没有视觉改动，没有 Kindle 真机验收。
- **下一步：**继续 P3-1，针对六卷混合样本实现并验证“卷边界、完整正文副本、空标题重复、同号不同正文”分离；先保留现有断点降噪证据，不把 1071 处探针数当作正确性结论。

### 2026-09-20 续：P2-2 EPUB 输入→MOBI 双模式边界合同

- **目标与退出条件：**明确 EPUB 输入转 MOBI 的两条路径：`保留原 EPUB 版式` 使用原 EPUB 的 spine 结构；`EasyPub 兼容重排` 只把可重排正文页导入为逻辑章节。两条路径都不能误用 TXT 章节树、TXT 全局正则或另一条路径的计数。
- 转换前检查现在按 MOBI 输入模式报告数量：保留原版式报告原 EPUB spine 文档数；兼容重排排除封面、导航页和没有正文节点的 spine 项，报告实际导入路径会生成的逻辑章节数。这样检查卡片与转换回执不会再因封面/目录页产生分歧。
- 新增双模式合同：先生成带 HTML 目录的 EPUB，再把一份属于 TXT 源文件的章节树故意挂到 EPUB 请求上，并把全局 TXT 正则设成完全不匹配；两种 MOBI 模式均成功，回执章数等于对应预检章数，TXT 树未被读取，源 EPUB 字节哈希不变。
- **验证：**`EpubInputMobiContractTests` **2/2** 通过，证据 `work/test-results/2026-09-20/p2-epub-input-contract.trx`；测试同时覆盖 KindleGen 产出的有效 MOBI 和源 EPUB 不被改写。Core 最终串行全量 **886/886**，证据 `work/test-results/2026-09-20/p2-epub-boundary-core-final.trx`。桌面稳定组合并行宿主出现 **40/43**（3 个失败都在测试自身枚举尚未创建的临时备份目录）；将 `RepairApplyWorkflowTests` 单独运行后 **11/11**，证据 `work/test-results/2026-09-20/p2-epub-boundary-repair-apply-alone.trx`。此前桌面稳定组合 **43/43** 的证据仍保留在 `p2-regex-desktop-stable.trx`。无视觉改动，无需截图验收；没有 Kindle 真机，设备目录仍待后续验收。
- **限制：**保留原版式的“章数”仍沿用原 EPUB spine 文档口径（可包含线性关闭的封面/导航项）；兼容重排按可导入正文页口径。两者是明确的不同输出模式，不把它们伪装成同一个目录。当前尚未把这一模式差异做成工作台的结构摘要。
- **下一步：**P2-2 的 Core 输出边界已通过；进入 **P3-1 卷边界、重复分类和同号不同正文** 算法验收。桌面并行宿主的备份目录竞态单独列为测试基础设施问题，不与本批 Core/EPUB 逻辑混报。继续保持无 Kindle 真机结论、无视觉改动。

### 2026-09-20 续：P2-2 EPUB / MOBI / 预览 / 转换前检查结构合同

- **目标与退出条件：**给同一份已保存章节树，EPUB、MOBI、预览和转换前检查都必须使用同一套章数、顺序、成品标题和正文范围；本轮只验证 TXT 输入的现代树路径，不改 Kindle 真机结论。
- 新增跨出口合同：自定义规则生成的章节树被手动改成“成品标题”，全局规则故意设置为完全不匹配；EPUB 仍按保存树输出标题和正文，预览条目与成品标题一致，转换前检查按保存树计数且不退回无树识别。MOBI 逻辑章数与保存树一致，KindleGen 前的优化包目录保留每个成品标题与目录目标，最终联合 MOBI 结构仍有效。
- 现状结论：`LegacyTextParser.ParseUsingChapterTree` 是 TXT→EPUB/MOBI 的唯一结构入口；MOBI 物理分片只改变文件承载，不改变逻辑目录。`BookPreviewService` 复用同一请求和章节树；`ConversionPreflightInspector` 在有树时关闭无树候选检测，按树校验。
- **验证：**新增跨出口合同 **2/2** 通过；新增合同后的 Core 串行全量 **884/884**，证据 `work/test-results/2026-09-20/p2-outputs-core-final.trx`。桌面稳定组合上一批 **43/43** 通过，证据 `work/test-results/2026-09-20/p2-regex-desktop-stable.trx`。没有视觉改动，无需截图验收；没有 Kindle 真机，设备目录仍待用户后续验收。
- **仍缺：**尚未把 EPUB 输入两种模式的差异做成工作台结构摘要。P2-1 的“能匹配但不能解析编号”能力提示仍待做成可见状态。
- **下一步：**补完 EPUB 输入→MOBI 边界合同后跑 Core 最终全量，再进入 P3-1 跨卷边界、重复分类和同号不同正文算法。

### 2026-09-20 续：P2-1 规则错误边界与缓存失效补齐

- **目标与退出条件：**补齐完整识别配置合同中“非法、超时、保存树重开不受当前全局污染”的边界；本轮不改识别启发式、不改视觉。完成标准是错误能指出具体规则并停止，保存树按书内快照恢复，缓存不复用过期手工树。
- 新增 `ChapterRecognitionRegex` 作为章节识别的统一入口：普通章节、卷/章/节层级和数字章节规则都使用 200ms 匹配上限；非法表达式报告规则名称，匹配超时报告规则名称及原文行号，避免把规则失败误报为“本书没有章节”。工作台保存/重新识别前先做同一套编译校验。
- 重开已有章节树时，若方案含完整识别快照，读取快照中的章节正则和层级设置后再解析源行；即使当前全局规则已经非法，也能按已保存方案恢复。旧项目没有快照标记时仍走原有全局兼容降级。
- `ChapterTreeDocumentCache` 的键由“源哈希/章数”扩为源、规则快照和条目内容指纹；同一本书手动改标题或区间而章数不变时，不会拿到旧的工作树。缓存只在同一版本、同一方案下复用，避免增加重复扫描。
- **验证：**Core 串行全量 **882/882**，证据 `work/test-results/2026-09-20/p2-regex-core-final.trx`；规则与快照针对性 **35/35**，缓存/快照针对性 **19/19**，桌面稳定组合 **43/43**，证据 `work/test-results/2026-09-20/p2-regex-desktop-stable.trx`。Core 重建前的 obj/bin 均移入 `work/build-backups/p2-regex-final-*` 并生成清单。桌面包含布局宿主的并行筛选会出现既有 0×0 定位干扰；将 `WorkbenchContextBarTests` 混入时曾出现该环境性失败，单独实际应用入口仍 **11/11** 通过，生产程序集编译成功。
- **仍缺：**规则能力面板尚未把“能匹配但不能解析编号”做成独立可见状态；P2-2 仍只完成 TXT→EPUB 的保存树合同，尚未完成 MOBI、预览窗口和转换前检查的逐项结构对账。没有视觉改动，无需截图验收。
- **下一步：**继续 **P2-2**，用同一份保存树核对 EPUB、MOBI、预览和转换前检查的章节数、顺序、标题及正文边界，然后再进入 P3-1 的跨卷与重复分类算法验收。

### 2026-09-20 续：P2-1 快照恢复补齐与 P2-2 输出结构第一道闸门

- 补齐备份完整状态快照：`SourceFullStateStore` 现在同时保存章节正则和完整层级/数字识别规则。恢复到完整书稿状态后，重建章节树不会丢失当时的识别上下文；旧版快照缺少这些字段时仍按无快照规则上下文兼容读取。
- P2-2 首个端到端合同：从带自定义规则的章节树方案生成转换请求，在全局规则故意不匹配的情况下转换 EPUB；输出章节数、成品标题和正文仍严格来自保存的章节树。转换器本身已经走 `LegacyTextParser.ParseUsingChapterTree`，没有暗中重新识别 TXT。
- 新增/补强回归：完整状态快照规则往返、保存树覆盖全局规则的实际 EPUB 输出。结构相关目标测试 **15/15** 通过；此前本轮核心串行全量 **877/877**、桌面相关 **17/17** 仍通过。证据 `work/test-results/2026-09-20/p2-fullstate-core.trx`、`p2-fullstate-desktop.trx`、`p2-structure-targeted.trx`。
- 限制：P2-2目前只锁住 TXT→EPUB的树路径，尚未逐项核对 MOBI KindleGen 包、预览窗口与成品目录的同一性；完整状态快照仍未扩展目录缓存/确认组等非树元数据。没有视觉改动。
- 下一步 **P2-2继续：EPUB/MOBI目录与正文边界对账**，补成品解析合同、预览合同和转换前检查合同，再转入 P3-1 的跨卷与重复分类算法。

### 2026-09-20 续：P2-1 项目保存、重开与批量转换按书隔离

- 本轮完成：章节树方案新增完整识别快照，保存章节正则和全部层级/数字规则；旧项目没有快照标记时继续沿用当前全局默认，保持兼容。重新打开已有方案时，保存的快照优先于当前全局设置；保存时用户切回默认正则的 `null` 也会被正确保留，不会复活旧自定义规则。
- 章节编辑器“应用并返回”现在把本次窗口读取的规则直接绑定到该书方案，不再把卷章层级、章节正则写回全局设置。全局设置仍作为新书和旧项目的初始值；数字规则对话框原有的全局范围选择继续有效。
- 批量转换、章节正文预览和插图定位均按每本书的识别快照取规则，避免书 A 的自定义正则污染书 B，也避免转换时重新使用最后一次编辑窗口的规则。
- 新增回归：保存方案重开优先使用书内规则；每本书的转换请求覆盖全局规则；项目文件往返保留章节正则和层级设置。Core串行全量 **877/877**，桌面章节编辑、批量、实际应用及恢复入口组合 **43/43**；证据 `work/test-results/2026-09-20/p2-book-rules-core-final.trx`、`p2-book-rules-desktop-final.trx`。Core重建前的 obj/bin已移入带清单的 `work/build-backups/p2-book-rules-*`。
- 限制：旧项目没有完整识别快照时仍按全局设置重开，这是兼容性降级；尚未把快照版本/能力提示展示到界面，也没有改变跨卷匹配算法。没有做视觉改动，因此无需截图验收。
- 下一步 **P2-2：章节树与转换成品结构一致性**，逐项核对保存树、预览、EPUB/MOBI目录和正文边界使用同一份树与识别上下文，再进入P3的跨卷错配、位移和缺标题算法修复。

### 2026-09-20 续：P2-1 识别规则随文档与重载保留

- 本轮完成：ChapterTreeDocument保存加载时的章节正则；WithEntries复制树时同时保留正则、层级设置和重复标题跳过统计。修复提案的版本指纹及校验采用文档中的实际正则，不再固定传空值。
- 写回后重新加载沿用原文档的正则与层级配置；源版本迁移在清空旧识别树之前保留这两项，外部修改后的重识别继续使用。默认正则的null表示也保留，避免用户切回默认后旧自定义规则复活。
- 用户效果：使用自定义正则的书，修复或重新读取后不会因本轮修复的参数丢失而突然按默认规则重识别；即便章节树外观相同，规则变化也能使旧方案的规则校验失效。没有新增按钮或改变视觉。
- 新增三条回归：仅正则变化而树相同；外部修改后保留自定义正则与层级；切回默认规则不复活旧规则。Core obj/bin先移至带文件清单的work/build-backups/p2-final-*再重建，日志确认生成Core DLL。
- 验证：核心串行全量 **875/875**，桌面实际应用/取消/故障恢复及备份入口组合 **17/17**；证据为work/test-results/2026-09-20/p2-rules-final-core.trx与p2-rules-desktop.trx。git diff --check通过。桌面没有重跑全量。
- 剩余：本轮补的是内存文档与重载传递，未证明项目保存/重开/转换的全部配置一致；规则能力说明、非法/超时规则提示仍待核对。没有改变识别/匹配启发式，也未解决已知跨卷误配，不把本轮当作44本算法验收。
- 下一步 **P2-1后半：项目保存与重开的一致性**，核对本书正则及数字/层级设置的保存、恢复和其他书隔离，随后进入P2-2章节树与成品结构一致性。P3算法修复、P4纸墨UI和P5完整验收仍按整体计划推进。

### 2026-09-20 续：P1-2 取消与备份窗口实际入口

- 修复确认取消：新增本地/目录两例，先在确认窗改变落地模式，再“暂不应用”关闭；核对原文字节、树标题、撤销栈与当前工作台模式均不变，没有新建修复快照。测试用路由事件触发，IsCancel默认命令未由合成事件执行时按取消结果关闭；不冒充物理鼠标/键盘验收。
- 备份窗口实际按钮：选择备份后触发“恢复此版本…”，在真实SourceRestoreWindow中分别点击“开始恢复”和“取消”。等待异步恢复结束，确认恢复得到原始正文，取消保留当前正文。补足此前仅调用RestoreSelectedAsync/事务API的入口覆盖。
- 新发现并修正：SourceBackupWindow在任何恢复失败后都附加“原文没有改动”，与替换后失败矛盾。移除这句统一追加，保持RestoreTransaction返回的阶段事实。
- 验证：实际应用与备份窗口组合 **17/17** 通过，证据 `work/test-results/2026-09-20/p1-cancel-restore-buttons.trx`。本轮只改Desktop反馈和测试，未重跑Core全量；最新核心全量仍为此前872项。
- 状态：P1-2主路径的零变化、完整决定、取消、写回后失败和备份入口已有证据；完整跨场景恢复、修后问题增减统计和多处插标题用户路径仍未验收，不宣布整个产品链完成。
- 下一步 **P2-1：识别配置一致性**。修后复查必须使用同一规则，所以先固定正则、数字标题与层级参数在检查/预览/重开之间的传递；P1-2剩余复查合同与P2一起补，完整用户路径继续列入P5验收。

### 2026-09-20 续：P1-2 真实写回后失败与备份恢复

- 新增真实WPF应用回归：在目录依据、允许写回且独立授权删除重复标题的模式下，点击实际应用按钮前将测试目录存储位置指向普通文件。源事务正常完成后，保存目录时真实抛出文件系统错误；不是直接调用反馈函数模拟文字。
- 验证原文确实已改、反馈明确“原文与操作前版本已不同”、旧树仍保留且旧坐标被阻断、修改前备份含树。随后使用RestoreTransaction从真实备份恢复，逐字节等于最初输入；再次校验后解除版本阻断。恢复前版本也另存备份，不手工覆盖文件来假装恢复成功。
- 实际应用流程 **9/9** 通过：`work/test-results/2026-09-20/p1-real-postwrite-failure.trx`。其中一条为本次新增故障恢复，其他八条为既有路径，不能全部算新增。
- 源事务/恢复事务 **22/22** 通过：`work/test-results/2026-09-20/p1-recovery-contract.trx`，包含替换后故障恢复不重复替换、版本冲突拒绝等既有合同。没有重跑核心/桌面全量，也没有修改生产代码。
- 限制：新增路径通过事务API恢复原文，未点击备份管理器恢复按钮；没有证明所有UI取消/迁移中断点，也没有自动修复跨卷误配。
- 下一步仍为 **P1-2收尾**：补“取消确认窗不应用、不覆盖当前会话选择”的真实交互合同，核对恢复入口与复查状态；完成后进入P2识别配置一致性。用户V3渐变水墨方向保持，UI实施另行截图验收。

### 2026-09-20 续：P1-2 零变化与失败反馈

- B01修复：TreeOnly不再无条件返回Changed=true，和写回模式无文本变化时共用TreeResult，依据真实树补丁判断。本次没有任何变化时明确显示“章节树与原文均无变化”，不创建事务。
- 两条新增核心回归覆盖TreeOnly/EditSource全部取消后无变化、无事务、原文字节不变。Core obj/bin备份并生成清单后重建，核心串行全量 **872/872** 通过（p1-nochange-core.trx）。
- 六卷混合基线复测：`work/baseline/20260920-172924-4a8600/baseline.json`，MIX-M1现在Changed=false，MIX-M3亦为false。目录跨卷误配未修，不能把状态修复当算法验收通过。
- 桌面失败反馈：事务NeedsAttention优先处理；异常或取消后核实磁盘哈希，分别说明原文仍为原版、已与原版不同、无法校验。不能再统一写“未应用修复”。已变化时提供备份位置和同步/恢复指引；与当前树不一致或无法读取时标记旧坐标不可继续使用。
- 无变化返回也检查磁盘版本，避免被外部修改拒绝之后仍显示正常。版本状态文案不再断定一定是“程序之外”修改，因为程序自己的写回也可能尚未同步树。
- 验证：实际应用/来源状态/失败反馈组合 **15/15** 通过（p1-result-feedback.trx）；新用例模拟已写入与未写入，并核对状态阻断、备份提示和磁盘字节不被反馈过程改变。不是对真实事务各中断点的完整恢复验收。
- 本轮没有布局/样式改动，未推送打包。P1-2仍在实施。
- 下一步：**P1-2提交中断与恢复**，覆盖写回完成但目录保存/树迁移失败的真实注入路径、恢复后重新检查及取消边界；通过后进入P2识别配置与成品一致性。渐变水墨视觉按已确认V3方向留待UI阶段。

### 2026-09-20 续：P1-1 统一修复会话接入

- 用户已确认V3纸墨精装视觉方向，补充主按钮使用渐变水墨；记录于设计说明，留待UI阶段细化。本批继续功能开发，不改样式。
- 新增ChapterRepairSession：分析开始前复制树条目/正文范围集合与参考目录节点集合；一次生成并持有提案、分卷选项和书稿快照。外部取得目录，显式本地请求不采用传入的目录取得结果。
- 预览保留完整决定副本，包括源文删除许可；提交必须与最近一次可应用预览的模式及决定一致。未预览、选择变化、范围变化均不提交。同会话只允许一次提交；源文件版本由原事务层在提交前重新校验。
- 主检查与目录面板共有的RunRepairReviewAsync接入Analyze→Preview→Apply，会话负责保管这些状态；界面不再分别创建提案或传递分卷开关。不是另写一套修复算法。
- 验证：Core obj/bin先移入带清单的备份后重建；核心串行全量870/870通过。桌面相关26/26通过，实际应用、模式切换、来源状态均经过会话入口。显式目录覆盖本地选项的实际窗口回归8/8通过（p1-session-explicit-catalog.trx）；与26项重叠，不相加。
- 证据：`work/test-results/2026-09-20/p1-session-core.trx`、`p1-session-desktop.trx`。没有重跑桌面全量，旧全量200是P0快照；本批未修改识别/匹配启发式，不宣称跨卷误配已解决。
- 限制：当前会话按WPF串行交互设计；P2仍需统一所有识别配置来源，P1-2仍需完善失败后恢复和零变化状态。取消与复杂原文恢复的完整用户路径仍须验收。
- 下一步 **P1-2 执行状态**：先修基线B01“零变化仍显示已更新”，再明确提交失败、已写入待恢复和复查结果。P1-1已接入核心会话，不再把会话缺失列为未实现。

### 2026-09-20 视觉评审续：网络参考与纸墨精装 v3

- 用户要求提升精致度并允许网络借鉴；查阅原研哉/MUJI及故宫数字文物库相关公开页面，参考来源写入设计说明。
- 新增 `docs/design/2026-09-20/EasyPub-Modern-纸墨精装概念-v3.png`：降低黄纸饱和度与纹理、以字级和网格建立层次、水墨收敛为页眉一处。v1/v2均保留。
- 这是待评审概念图，未实施生产UI。图中工作台带全局导航的容器结构须另行确认可用性，不自动推翻开发方案的独立工作台。
- 下一步：根据用户反馈确定视觉方向，再制作准确可交互原型；既有P1-1与后续算法计划保持。

### 2026-09-20 视觉方向评审：黄纸油墨 v2

- 用户认可目标稿布局，要求保留青绿版本作为未来主题候选，当前转向更鲜明但简洁的黄纸油墨。
- 已交付主窗口与章节工作台两屏全局概念图：`docs/design/2026-09-20/EasyPub-Modern-黄纸油墨全局概念-v2.png`。暖纸底、墨块按钮、边角淡墨山形与枝影，方案说明随图保存。
- 本轮是用户要求的视觉探索，没有改生产代码、没有发布，P1-1功能任务仍保留。图片为概念，不能当作实际窗口验收。
- 下一步：用户评审v2后确定视觉规范，再将准确布局与风格落实到可操作原型；P1-1统一会话工作继续按总计划推进。

### 2026-09-20 续：P1-1 保留目录时独立选择本地依据

- 用户操作：主检查按钮下新增「本次仅按本地检查」。勾选后按钮、依据说明和下一步提示同步切为本地；取消后恢复使用已保存目录。切换不删除目录、不改树、不撤销原来的目录修复。
- 执行：入口先捕获本次选择，本地直接使用 NotRequested + CurrentTreeHeuristicRequest，跳过 ReadSavedCatalog。Core显式请求路径不抓目录、不联网。这里“不读目录”限定为本次建议生成，不是承诺工作台的目录状态显示从不读保存记录。
- 目录面板明确选择某份目录并生成建议时，会取消本地选项并使用那份给定目录，避免界面仍写本地却执行目录修复。该选项仅在当前工作台生效，不新增全局自动偏好。
- 验证：真实应用流程8项通过，其中有保存目录的本地路径检查 CatalogFound=false、原目录逐文件字节不变，勾选/取消后按钮依据跟随变化。后续应用、忘记目录、来源状态组合回归15/15通过；正式主题截图单独1项通过。均非完整桌面全量复测。
- 证据：`work/test-results/2026-09-20/p1-local-regression.trx`、`p1-local-visual-final.trx`。截图 `work/screenshots/p1-local-basis-final/P1-本地依据保留目录-主题.png`，使用隔离的全新设置，不读用户偏好。早期无主题截图不可作为视觉验收。
- 视觉待用户确认。本批未改Core算法、未动真实TXT、未推送、未打包。
- 下一步仍为 **P1-1**：把依据、范围、提案和决定封装为统一会话，确保预览/应用读取同一快照；补目录面板显式覆盖本地选择与取消路径的完整交互合同。随后 **P1-2** 处理零变化误报和执行状态。不能把新增开关等同整个P1-1已完成。

### 2026-09-20 续：P1-1 默认范围与工作台模式一致性

- 为什么：全局默认为写回；工作台底栏使用本窗口缓存，但每次确认窗重新读全局默认，导致同一本书上一次选择与下次窗口默认可能不同。
- 修改：新用户、旧设置缺少模式字段时默认只改树；未知枚举值也回退只改树。有合法历史值的设置保留，包括 EditSource。旧文件没有标记用户是否主动选过，无法把历史写回值都称为“显式偏好”，因此不猜测覆盖。
- 迁移不写盘：同步/异步读取一致，读取旧文件不修改其字节；之后正常保存再保留解析出的模式。不修改用户真实设置文件。
- 用户行为：同一工作台应用过某个模式后，再开确认窗沿用这个模式，与底栏一致；仍可在确认窗切换。取消不持久化新模式，程序没有自动保存全局偏好。
- 验证：Core先将obj/bin移入带文件清单的work/build-backups，再构建确认生成Core DLL；核心串行全量 **867/867**，新增5项覆盖新安装/缺字段/两种历史有效值/未知值及读写一致性。桌面相关 **22/22**，含8条实际应用流程，两条模拟本窗口模式与全局值相反并核对底栏、确认窗、实际TXT影响。
- 证据：`work/test-results/2026-09-20/p1-mode-core.trx`、`p1-mode-desktop.trx`。本批没有重跑完整桌面套件；之前200项全量是P0快照。
- 范围：没有布局或样式改动，未新增算法启发式；没有修改用户TXT、推送或打包。P1-1仍未全部完成，尚缺可显式选择本地依据的界面入口与统一会话对象。
- 下一步 **P1-1 的依据选择**：保存过目录也能选择“本次只按本地检查”，保留目录记录；确认该路径不读目录、不联网。界面变动需提供实际截图让用户确认，然后进入P1-2执行状态修复。

### 2026-09-20 续：P0-2 测试快照完成

- 当前同一代码快照：Core串行全量862/862，Desktop隔离发现/执行/通过均200。
- 保留失败：Core默认并发两轮均861/862，性能项521/491ms超400ms；同项单独231ms，串行全量230ms。没有修改门槛，不能声称默认并发稳定全绿。
- 证据、复现命令和边界：[当前代码测试快照](2026-09-20_当前代码测试快照.md)。本轮已重跑完整桌面套件，旧189项不再作为最新基线。
- P0-1/P0-2基线工作完成，完整产品验收未完成；**下一步P1-1：统一四模式会话、默认只改树和旧设置迁移**。之后P1-2处理执行状态，P3处理跨卷弱匹配。


### 2026-09-20 续：P0-1 独立场景完成，进入 P0-2

- 完成：生成 S1～S9 单缺陷样书及 S0 无缺陷对照，每份包含690章真值、输入哈希和逐章正文哈希；不覆盖既有混合样本。S9 改用真正的“九→久”单处错误，S3 断言21章实际移动。
- 运行：36个缺陷组合及4个对照组合均完成，所有现存独立正文仍覆盖。结果、预期、不适用行为及证据路径见[独立场景报告](2026-09-20_独立场景四模式基线.md)和[样本合同](../samples/章节独立场景/说明.md)。默认基线不替代UI选择、重开、输出和删除授权验收。
- 定位：卷标题完整时 S3 目录树恢复顺序；S5/S6/S9 单独即可引起跨卷误配。后续算法工作聚焦卷作用域和弱匹配回退，不凭全书同章号借用正文。
- 生产算法与界面本批未改。诊断入口增加显式 --fixture，保存本次输入位置；样本生成器独立于生产识别器。
- 整体计划：P0-1 基线子任务完成；当前 **P0-2 全量测试快照**。核心首次861/862通过，长文本性能521ms超过400ms目标，保留失败报告；最终核对见下方 P0-2 结果。
- 下一步：完成并记录 P0-2，然后进入 **P1-1 四模式会话与默认模式**；P1-2修零变化执行状态，P3处理基线暴露的跨卷匹配。

### 2026-09-20 续：P0-1 四组合混合基线落盘

- 完成：新增 `tools/repair-baseline`，独立临时副本执行四组合，记录识别配置、实际决定、原文哈希、最终树、逐章正文哈希覆盖和阶段耗时。
- 核验：原目录与去 Markdown 前缀目录各运行四组合。后者明确解析成 6 卷 690 章。样本没有改动，现存 679 份独立正文均保留。
- 失败：目录模式存在跨卷误配，例如卷 5 第 30 章正文归到卷 4 第 30 章；本地树模式零变化仍返回已更新。没有将运行成功标记为验收通过。
- 记录：[基线报告](2026-09-20_六卷四组合基线报告.md)，包含两轮数据、诊断边界与 B01～B04 分派；[验收台账](章节重构验收台账.md)已更新四个 MIX 状态。
- 本批没有修改生产算法或 UI，也没有重跑单元测试；基线运行成功不能替代全量测试。
- 下一步：**P0-1 独立场景基线与反例**，再执行 **P0-2 当前快照完整测试**。整个 P0-1 尚未完成，不能跳到打包交付。

P0 已启动，整体重构尚未完成，未发布新版本。没有修改用户 TXT、旧样本或原仓库，没有推送。

**最后核对：2026-09-20。本文件持续追加，不另建同名进度副本。**

**整体计划：**[开发方案 §13.5](2026-09-19_章节编辑与修复完整开发方案.md#135-整体执行计划与当前任务2026-09-20-建立)。**当前主任务：P2-1 识别配置一致性**；[36 格与混合样本验收台账](章节重构验收台账.md)。每轮按开发方案 §14.5 写明当前任务、完成、验证、剩余与下一步。

| 当前已完成的闭环 | 验证状态 |
| --- | --- |
| 确认窗完整决定、取消后重新选择、删除原文单独许可 | 已有真实控件回归 |
| 来源随树撤销、写回后隔离旧行号历史 | 已通过 |
| 四组合重复清理、两条实际删除 TXT 流程 | 真实应用按钮 6 项通过，含成品正文覆盖、备份和撤销边界 |
| 补章与改名同时写回后的完整树映射、多插入操作身份隔离 | 最新核心全量 862 项通过；此前桌面相关 19 项通过 |
| 性能 | 六卷树差异缓存已有测量；完整树迁移改为按坐标映射，未测全流程收益 |

最新证据：[核心 862 项](../work/test-results/2026-09-20/multiple-insert-identities-core.trx)、[此前桌面相关 19 项](../work/test-results/2026-09-19/confirmed-tree-desktop.trx)、[真实按钮与正文覆盖 6 项](../work/test-results/2026-09-19/actual-apply-body-verified.trx)。它们覆盖范围不同，不能相加作为测试总数。以上为早期批次证据，最新全量结果见顶部 P0-2 记录。

**未完成：**真实区段移位、多处插标题、自定义正则配置一致性、44 本语料、最终 UI 人工确认及打包。最新桌面隔离全量200项通过；核心并发性能失败与串行通过结果均保留。

## 修复确认窗的决定传递

- 为什么：原文删除许可在 UI 回调中丢失；生产入口取得完整决定后又只传选中动作，Core 再生成的决定不含该许可。
- 现在：确认窗直接形成完整决定；预览回调和生产应用入口接收同一组决定。提案在打开确认窗前创建一次，避免每次勾选重新构造。
- 用户操作：勾选重复清理，再勾“并从原文删除这一份”，原文影响预览随之变化；应用后删去明确选择的范围，保留正文与修改前备份。
- 同时修正：本地方案的 Entries 是原树，不能作为已应用预览；重复清理产生的 RemovedEntry 应归入 RemoveDuplicate 操作组，否则整行会被隐藏。
- 验证：新增真实 WPF 勾选→决定→预览→Core 源事务→核对 TXT 和备份测试。相关回归通过，后续仍需完整主工作台模态点击及四组合验收。

## 性能：复用章节树差异

- 为什么：切换落地模式、勾选源文删除许可不会改变成品树的选择集合，没必要重新生成同一份树差异。
- 现在：确认窗按动作 Key 集合缓存树差异；动作选择改变即失效。模式和源文许可仍重新校验源补丁，缓存不跨窗口、不跨提案。
- 验证：回归断言切换模式复用同一差异对象，取消动作必须失效，源文预览随实际决定变化。
- 限制：尚未完成 44 本语料耗时对比，不能宣称某个加速百分比；后续继续检查编译和正文比较热点。

## 当前树来源

- 为什么：本地修复也调用 ApplyRepairEntries，原先会无条件标为目录校验后。
- 现在：只有明确使用目录的方案更新目录来源，保存真实目录指纹；本地修复保留现有历史。
- 验证：本地树→本地修复仍是本地；目录修复后→再本地修复仍保留目录历史。
- 来源已纳入撤销快照，详见下节；完整会话模型仍需后续统一。

## 9 月 19 日续：撤销与原文版本

- 为什么：ApplyRepairEntries 原来先改文档和来源，再进入 Mutate，旧来源没有进入快照；只核对目录而树条目不变时，甚至没有可撤销记录。
- 现在：文档与来源在同一次 Mutate 内替换；快照保存来源，比较也包含来源。目录核对后点“撤销”恢复本地来源，点“重做”恢复目录来源。
- 原文版本边界：原文哈希变化后，在 Mutate 完成后清空旧历史，避免 Mutate 再次把旧行号压回撤销栈。此时不能用树撤销还原旧 TXT，原文恢复继续走备份机制。
- 验证：先复现来源撤销失败，再修复通过；另用临时 TXT 验证换版本后撤销、重做按钮均不可用，旧树不会被恢复。

## 9 月 19 日续：性能实测

新增 Six_volume_preview_cache_preserves_diff_and_reports_cost，用新六卷样本，在同一确认窗交替执行强制重算与缓存命中，各 12 次。每次核对差异内容一致，命中返回同一差异对象；没有在测试里设置不稳定的速度阈值。

样本识别出 668 个条目，预览差异 638 项。这些数字是现有算法的观测值，不代表六卷样本修复验收通过。

| 测量 | 重算中位耗时 | 缓存中位耗时 | 重算平均分配 | 缓存平均分配 |
| --- | ---: | ---: | ---: | ---: |
| 全套同进程运行中的测量 | 9.284 ms | 0.073 ms | 2,664,062 B | 32,400 B |
| 独立测试组中的测量 | 4.630 ms | 0.062 ms | 2,685,648 B | 32,400 B |

仅包含树差异计算，排除构窗、源补丁编译、绘制、网络。耗时随测试宿主和机器负载变化，不能把这一比例宣传为全流程加速。44 本语料基线仍待测量。

## 9 月 19 日续：桌面测试宿主与基线

- 原始单进程全套：189 项，158 通过、31 失败。多项窗口尺寸为零、找不到控件；另有裸 Application 与真实 App 资源互相影响。
- 尝试延长 Application 生命周期又暴露跨线程访问，已撤回，未改产品代码来迁就测试。
- 新增 tools/test-desktop-isolated.ps1：自动发现全部测试，按类使用独立 testhost，依赖不同 Application 的样式测试逐方法隔离；保留全部原断言。输出逐组 TRX，并核对发现/执行数量，不允许漏测冒充通过。
- 独立进程运行 33 组，合计 **189 / 189 通过**。证据位于 work/test-results/desktop-isolated-20260919-192709/；失败的原始全套记录位于 work/test-results/2026-09-19/chapter-refactor-desktop.trx。
- 这建立了可复现的隔离测试入口，普通单进程 dotnet test 的 WPF 宿主共享问题仍存在；后续可统一单一长驻 UI 线程，不能把当前结果描述成单进程全绿。

## 旧红测试的确切原因

`Imported_txt_is_automatically_analyzed_without_opening_the_preflight_window` 已复现，实际问题码是 `Error:kindlegen_missing`。

测试在异步启动配置加载结束前选择 EPUB，随后启动旧配置覆盖为 MOBI。测试现在先等待 `_optionTrackingReady` 再选择 EPUB，原断言通过；保留问题明细断言帮助后续定位。没有删掉 MOBI 必需条件检查，也没有放宽预检结论。

## 690 章样本

新目录 `samples/六卷690章修复验收/`：完整原稿、损坏原稿、参考目录、独立真值清单。生成器核对 690 个预期章、690 个正文实例、679 个不同正文、11 个重复实例及每份正文哈希。

它验证真正的区段移动，不能与旧样本插入副本混淆。生成器与生产识别代码独立。四组合完整结果尚未运行。

## 早期验证记录（保留历史，最新状态见顶部）

- 初始落地模式与自动检查基线：5 通过 / 1 失败；失败码查明如上。
- 落地模式回归、确认窗、工作台来源、自动检查定向组合：17 通过（加入缓存用例前）。
- 加入缓存用例后，落地模式 + 确认窗 + ChapterBatchTests：34 通过。
- 新样本生成时数量与正文哈希断言通过。
- 本轮 Core 全量 **855 / 855 通过**，桌面按进程隔离 **189 / 189 通过**；普通单进程桌面宿主问题见上节。Core 结果：work/test-results/2026-09-19/chapter-refactor-core.trx。
- 44 本语料性能、四组合完整结果、界面人工截图确认与真机验收尚未完成。

## 后续已完成的实施批次（新批次在前）

### 2026-09-20：整体计划落实与验收台账建立

- 当前任务：P0-1，建立可追踪的验收基线；本次完成总体计划和覆盖台账，不是完成整个 P0-1。
- 本次完成：把原开发方案 P0–P5 拆成 15 个有编号、状态、退出条件和后继任务的执行项，写回同一开发文档 §13.5；§14.5 规定每轮必须写下一步。没有再另起互相竞争的整体方案。
- 台账：新建章节重构验收台账，列出 36 格和 4 个混合样本运行项。重复标题的真实 UI 用例只标“局部通过”；底层补入、插入 ID 和性能用例均不冒充整类问题验收。
- 核对依据：原方案 §12.2、§13.1、§14.1；当前源码默认落地仍为 EditSource，尚未找到完整 ChapterRepairSession/RecognitionContext 实现，因此 P1 只标部分实施，不宣称完成。
- 验证：本轮仅更新计划与文档，核对编号、36 格数量、阶段顺序和证据路径；没有运行新代码测试，最新核心证据仍为 862 项。
- 剩余：混合样本四组合还没有生成完整基线结果；完整 Desktop 最新快照仍需重跑。
- **下一步：P0-1 的 MIX-M1～M4 基线运行入口与结果报告**，只操作临时副本，列出失败、不确定项和正文哈希差异。随后 P0-2 全套测试，再收尾 P1 的依据选择与状态模型。中途修安全缺陷后返回本任务，不继续无序扩展。

### 2026-09-20：实施记录入口修正与多处插入定位

- 用户指出实施记录没有更新后，实际核对发现近几轮已经落盘，但完成批次误放在“接下来”下面，顶部测试汇总仍是早期 855 项。已重排标题、增加当前状态表、单列剩余工作，并在 HANDOVER.md 顶部加入此记录的链接，保留旧交接的历史事实。
- 逐份解析现存 TRX，确认此前核心 861 / 861、桌面相关 19 / 19、真实按钮 6 / 6；没有把相互重叠的测试数量相加。
- 本轮代码问题：RepairSourcePatchCompiler.Project 对每个动作都从 ins-0 开始编号。两个不同动作各插一个标题时只有一个不同操作 ID；按操作 ID 查询插入后行号会冲突。
- 修复：插入操作 ID 包含 actionId 和动作内序号，重复编译仍生成相同 ID，不同动作不会混用。既有事务中的已保存 ID 不重写。
- 新回归 RepairInsertIdentityTests 先红（预期 2 个不同 ID，实际 1 个），随后核对两个标题分别插入到新第 1、3 行，原正文顺序和内容保持不变。
- 范围：本次只验证效果投影→源渲染→行号映射，不代表“缺标题自动定位、两处插标题的完整工作台流程”已完成。算法改动是身份生成，不改变识别启发式或章节位置推测。
- 验证状态：Core obj/bin 已保存清单并备份移开，重新构建；核心全量 **862 / 862 通过，零跳过**。报告 work/test-results/2026-09-20/multiple-insert-identities-core.trx。git diff --check 通过；没有改 UI、发布或推送。

### 续：写回后保留完整确认树

- 新复现：当前树漏第二章，同时按目录收录第二章并修改第一章标题。源 TXT 改对，但最终树只剩第三章。原 MaterialisePlan 在旧身份表找不到新增节点就跳过；改名节点已被迁移层排除，又无法通过旧标题找到。
- 修改：在写入前，用 SourceCoordinateMap 将编译生成的完整 RebuiltEntries 映射到新行号。保留确认树的顺序、层级、标题和正文范围；插入标题按原锚点及操作定位，禁止全书按同名猜测。删除行不再进入正文，末尾逻辑空行单独映射，连续新行合并成范围。
- 新树在原文替换前完成 ValidatePlan；失败直接拒绝写入，不再静默返回丢失已确认修改的迁移树。迁移流程仍负责版本状态与旧身份报告。
- 性能：移除旧逻辑逐动作 FindIndex 和按标题扫描，映射过程为节点数与所覆盖行数的线性遍历（重叠范围按实际覆盖总量计）；未给出未经实测的全流程加速比例。
- 回归核对三章顺序、第一章新标题、第二章新收录以及每章正文归属。核心全量 **861 / 861 通过**，报告 confirmed-tree-materialisation-core.trx。上一节的 RemoveEntry 定点补丁已由此次完整确认树映射取代。
- 真实工作台应用与确认窗决定传递组合 **19 / 19 通过**，包含前一轮的 6 条重复清理/真实写回流程；报告 confirmed-tree-desktop.trx。
- 仍待验收：多处正文内插标题、自定义正则全配置、六卷真实章节移位、44 本语料误报与性能。当前并不声称这份样本已全部修复。

### 续：真实应用按钮与同名章节误删

- 新增 RepairApplyWorkflowTests：打开真实工作台，进入统一确认窗，明确勾选重复清理，实际点击“应用所选修改”。设置、目录、原文与备份都隔离在临时目录，保留验收证据。
- 4 种组合在不授权 TXT 删除时通过：树清理生效、TXT 字节不变、备份和移除清单存在、撤销恢复章节及本地来源。目录方案不默认勾选重复清理，测试明确模拟人工勾选，未改变产品默认。
- 再加入有/无目录的实际 TXT 删除，先出现 2 个失败：迁移后树丢失本应保留的同名章节。根因是 MaterialisePlan 重放 RemoveEntry 时按标题兜底定位，已被迁移删除的副本又匹配到了保留副本。
- 修复：删除动作只用 SourceEntryRebind 的明确旧/新身份映射；已消失的身份不再次删除，不按相同标题猜目标。其他动作的物化逻辑未在此轮整体重写。
- 修复后真实按钮 6 / 6 通过；补充检查成品树正文覆盖。TXT 写回后旧树撤销不可用，原文状态正常，修改前树快照仍在。
- Core 全量 **860 / 860 通过**：work/test-results/2026-09-19/source-delete-identity-core.trx。Core obj/bin 已备份并移开后重建，确认输出新 DLL。
- 仍非全部六类缺陷验收：当前闭环覆盖重复标题清理及写回许可；真实区段移位、复杂新增节点与 44 本语料仍待推进。

### 续：写回模式也能应用只改树的动作

- 根因：ChapterRepairApplier 在源补丁为空时返回 Changed=false；工作台随即退出。选择“同时改原文”但未授权删除 TXT 中的重复内容时，连成品树的重复项也无法移除。新增有/无目录回归先复现了两个失败。
- 修改：源补丁为空或渲染字节完全相同时，根据树补丁报告实际变化，不创建无意义的文本事务。编译结果保留原坐标中的 RebuiltEntries；工作台只在没有文本事务时采用这份树，发生写回仍使用迁移后的树。
- 两种模式现在走同一个 ApplyAsync 入口和完整 decisions；工作台不再额外调用 RebuildWithSelection 生成另一份应用结果。减少了一次树重建，尚未测量全流程耗时收益。
- 移除清单改用本次编译的 RemovedLines，避免直接使用默认全选方案的清单。两处重复只选择一处的回归核对实际覆盖差与清单相同。
- 用户操作：选择“同时改原文”→勾选重复清理→不勾“并从原文删除这一份”→应用。成品树移除副本，TXT 不变；执行前保存快照及逐行清单，树调整可撤销。
- 核心构建：先将 Core obj/bin 移入 work/build-backups 并保存文件清单，再重新构建；日志确认输出新的 Core DLL。
- 验证：核心全套 857 / 857 通过（tree-only-in-write-mode-core.trx）；随后扩充四组合与部分选择，ChapterRepairApplierTests 18 / 18 通过。四组合目前只覆盖这一种重复清理场景，六类缺陷完整验收仍未完成。
- 桌面定向组合 49 / 49 通过（unified-apply-desktop.trx），覆盖确认窗、模式决定、批量操作、来源撤销和统一入口约束。本轮没有重跑全部桌面隔离套件。
- 限制：完整主工作台模态“应用”点击与落盘回归仍需补齐；既有真实写回后的复杂新增/重排物化逻辑没有在本轮重写。

### 续：取消建议后仍能重新选择

- 问题与证据：确认窗把已选择的树差异当成待选清单，取消后整行消失。有/无目录 × 只改树/同时改原文的 4 条新窗口回归均先失败。
- 修改：可执行建议保留在清单中，未选中也能再次勾选；缺正文、卷备注等只读事实不变成可执行动作。同一个动作只出现一个选择框，避免重复决定。
- 用户操作：取消某项后仍可在原位置重新勾选；全部取消时摘要和详情说明不执行，应用按钮禁用。源文删除许可只有在所属动作选中时可操作。
- 说明修正：本地建议不再声称来自参考目录；写回模式说明改为“写回后通过备份恢复”，不再承诺能用树撤销恢复 TXT。
- 性能：切换落地模式原来先直接编译预览、再 Refresh 编译一次，现在只经过 Refresh 一次。保留此前的树差异缓存。
- 验证：LandingModeWiringTests + RepairReviewWindowTests 合计 **19 / 19 通过**；含 4 组合取消→从真实控件重新选择→预览恢复、TXT 字节不变。结果 work/test-results/2026-09-19/selection-roundtrip.trx。
- 截图：work/screenshots/2026-09-19-selection-final/修复确认-取消后保留建议.png，真实 WPF 窗口与应用资源，等待用户确认；没有做布局重排。
- 范围限制：这是四组合的选择交互回归，不是四组合完整应用验收。已定位写回模式 SourcePatch 为空时直接返回，导致树变化也被跳过；下一步修统一执行与基于实际选择生成移除清单。

## 当前下一步

P0基线工作已完成；当前进入 **P1-1 四模式统一会话**，之后按开发方案推进 P1-2统一执行、P2 的识别配置与成品一致性、P3 的分卷/重复/移位/缺标题算法、P4 界面确认、P5 全量语料与打包。每步记录完成证据和下一任务，不把已通过的局部回归当成整阶段完成。
