# Phase 4a/4b 实施记录 · 三维影响与补丁规范化

> 依据：`docs/2026-09-16_修复流程重构设计方案-v3.3.md` §15.1 的 Phase 4（前半）
> 前置：Phase 0/1 见 `…Phase0-基线与回归夹具.md`；Phase 2 见 `…Phase2-备份层实施记录.md`；Phase 3 见 `…Phase3-版本绑定与决策实施记录.md`
> 夹具：`samples/六卷缺陷样书/缺陷样书.txt`（SHA-256 `69D7223…F926`）
> 本阶段全部只做内存，不写盘。

---

## 0. 本阶段做到哪、没做到哪

Phase 4 的目标是"能说清一件事会怎么改"。做到的是：**说得清的就精确描述，说不清的就明说不知道**。
后半（把动作编译成真正的 `TreePatch`、把渲染字节算出来、让两种落地模式在同一份带正文的数据上对照）
留给 4c。

---

## 1. 交付内容

| 文件 | 内容 |
|---|---|
| `RepairEffect.cs` | 三维判别类型、`BodyAssignment`、`SourceAnchor` 判别联合 |
| `CanonicalChapterModel.cs` | 规范化章节模型与 `SourceBinding` |
| `SourcePatch.cs` | 物理改动补丁与 `SourcePatchNormalizer` 的八条规则 |
| `RepairEffectCompiler.cs` | 动作类别 → 三维影响 |

### 1.1 三个维度分开的理由

它们可以不同，所以不能合成一个：

| 动作 | 树形态 | 文本内容 | 正文归属 |
|---|---|---|---|
| 收录一个已定位的标题行 | 增加条目 | **不变** | 不变 |
| 改标题 | 改标题 | **替换一行** | 不变 |
| 摘掉重复副本（只从成品） | 移除条目 | **不变** | **变** |
| 把一行立成标题 | 增加条目 | **不变** | **变** |

每一维都有 `Indeterminate`，而且必须由编译器**显式写出来**。这条设计是整层的支点：一个没人
定义过效果的新动作，不能悄悄退化成"没有影响"，而要说明自己没定义 —— 于是 `EditSource` 拒绝它，
`TreeOnly` 照常可用。

### 1.2 规范化模型装不进存储细节

`CanonicalChapter` 里没有行号、没有 id、没有"虚拟/物理标题"标志。这些恰恰是两种落地模式**必然
不同**的东西：`EditSource` 删行插行之后每一个物理行号都变了。把它们放进模型，等于让等价性检查
从一开始就不可能通过。

正文哈希按「章节内第几行 + 该行文字」算，不按绝对行号。位置参与是为了区分"同一句话在正文里出现
两次"；不含绝对行号是为了让整段行号平移不改变结果。

---

## 2. 六卷样书实测

`dotnet run -c Release --project work/catalog-fix-sim/sim.csproj -- effect`

```
=== 1. 三维影响（样书真实动作）===
   AddChapter         10 项  能完整描述   10
   DemoteExtra         6 项  能完整描述    0  还说不清：正文归属
   KeepExtra          32 项  能完整描述   32
   MissingBody        41 项  能完整描述   41
   RemoveDuplicate    32 项  能完整描述    0  还说不清：正文归属
   Retitle            69 项  能完整描述   69
   VolumeNote          1 项  能完整描述    1

   每个动作要么完整描述，要么说出哪一维不知道

   例（说得清）Retitle
      树   标题改为「第四十章 丙40 古渡破晓」
      文本 改第 783 行：「第六十四章 丙64 重逢古渡」→「第四十章 丙40 古渡破晓」
      归属 不变
   例（说不清）RemoveDuplicate
      树   移除条目
      文本 不变
      归属 不确定  ← 要等整棵树算出来才知道

=== 2. 补丁规范化（用真实的前置条件）===
   69 个替换，全部前置条件取自样书真实行 → 接受
   故意让同一行既被改又被删 → 拒绝：前置条件不符：第 783 行不是预期的内容（预期「内容」，
        实际「第六十四章 丙64 重逢古渡」）；同一行被多个操作占用：第 783 行同时被 2 个动作要求改动
   前置条件与样书不符 → 拒绝：前置条件不符 …

=== 3. 规范化模型：行号无关 ===
   修复前 468 章，修复后 444 章
   结构确实不同（模型看得出来）：True
   同一模型自比相等：True
   把每个行号整体 +7 之后仍然相等：True
```

**第 1 项里的两个 0 不是失败**：`RemoveDuplicate` 与 `DemoteExtra` 都会搬动文字，而"搬完之后这段
文字归谁"要等整棵树算出来才知道。现在说不知道，等 4c 能算了再说知道 —— 而不是现在猜一个。

**这一点探针一开始暴露成真问题**：第一版给它们传了 `BodyAssignment.Empty`，于是报告「改归 **0 个
区间**」—— 一个具体但**错误**的答案，读起来像"这段文字从此没有归属"。改成传 `null` 之后才走到
诚实的那条路上。

---

## 3. 两个实现陷阱（都是"看起来对"的那种）

### 3.1 基类的 `Ordinal` 被位置参数遮蔽

```csharp
public abstract record SourceAnchor { public int Ordinal { get; init; } }   // ← 这一行有害
public sealed record AfterOriginalLine(int Line, int Ordinal = 0) : SourceAnchor;
```

派生记录的位置参数 `Ordinal` **遮蔽**了基类同名属性。于是通过基类读 `anchor.Ordinal` 永远是 0，
而真实值躺在派生记录上。排序看起来在工作 —— 所有插入都被当成 ordinal 0，然后按操作 id 排。

只有当测试真的去区分两个**不同 ordinal** 的插入时才暴露。修法：基类不声明它，改由
`SourceAnchor.OrdinalOf(anchor)` 按类型读。

### 3.2 用 `0` 当"这个操作不针对行"的哨兵

规范化器第一版这样判断操作针对哪一行：

```csharp
var line = operation switch { DeleteOriginalLine d => d.Line, ReplaceOriginalLine r => r.Line, _ => 0 };
if (line == 0) continue;   // ← Delete(0) 这个越界请求被静默跳过
```

`0` 是**非法行号**，不是"不适用"。用它当哨兵，越界检查就恰好漏掉了最该抓的那个值。
参数化测试里 `line: 0` 那一格立刻红。改成按类型 `switch`，不适用就用 `continue` 明确跳过。

---

## 4. 补丁规范化的八条规则

全部在**写盘之前**检查，违规即拒绝。次序不是用来消解冲突的 —— 那是旧做法，它把错误藏起来：
两个动作对同一行有不同意见时，产出的文件同时不满足它们俩，而没有任何地方会说这件事。

| # | 规则 | 违反时的报告 |
|---|---|---|
| 1 | `Delete ∩ Replace == ∅` | 同一行被多个操作占用 |
| 2 | 同一行最多一个替换 | 同上（并区分"2 个动作"与"同一个动作") |
| 3 | 同一行最多一个删除 | 同上 |
| 4 | 插入按 `(锚点, ordinal, 操作 id)` 稳定排序 | —（由排序保证） |
| 5 | 每个删除/替换的预期旧文本与当前一致 | 前置条件不符 |
| 6 | 所有行号在 `1–行数` 之内 | 行号越界 |
| 7 | 插入锚点不得落在被删除的行上 | 锚点落在被删除的行上 |
| 8 | 不同动作不得产生互相矛盾的物理操作 | 同一行被多个操作占用 |

第 7 条来自样书的真实形状：「把副本删掉，再往它原来的位置插一个标题」读起来通顺，实际不成立 ——
那个锚点不存在了。

---

## 5. 测试

| 文件 | 条数 | 要点 |
|---|---|---|
| `CanonicalChapterModelTests.cs` | 11 | 行号无关、正文敏感、父路径、卷与章节之分、样书实测 |
| `SourcePatchNormalizerTests.cs` | 17 | 八条规则逐条、顺序无关、全序稳定、多问题并报 |
| `RepairEffectTests.cs` | 17 | 每类动作要么完整要么说明、前置条件取自原文、样书实测 |

**Core：506 → 551，全绿。**

三条值得单说的：

1. **`Every_action_kind_is_either_fully_described_or_says_what_is_unknown`** —— 反射遍历所有动作
   类别，断言"要么三维都具体、要么至少一维是 `Indeterminate`"，且**声称不确定时确实有一维是
   `Indeterminate` 类型**。防止将来加类别时写出"嘴上说不确定、类型上却给了个具体值"。

2. **`The_order_operations_are_supplied_in_does_not_change_the_patch`** —— 渲染是单遍的，所以
   传入顺序不得影响结果。这条是"次序只为可读"这一设计的守卫。

3. **`Inserts_sharing_an_anchor_come_out_in_a_fixed_order`** —— 同锚点插入若有并列，渲染出的字节
   就取决于调用者凑巧怎么建列表。全序是硬要求。

---

## 6. 4c：无损文本模型与补丁渲染（已完成）

### 6.1 交付

| 文件 | 内容 |
|---|---|
| `SourceTextDocument.cs` | 逐行保留各自换行符（CRLF / LF / CR / 无） |
| `SourcePatchRenderer.cs` | 单遍渲染，按原始行号索引；`SourceRenderVerifier` 无损校验 |

### 6.2 为什么无损是硬要求

允许改原文之前，先把计划对着**原始字节**渲染一遍并比对：不带任何改动的渲染若不能原样还原文件，
说明编码或换行还没被理解到可以下笔的程度 —— 此时应当拒绝，而不是存回一个会悄悄变得不一样的文件。

旧模型（`ChapterEditingDocument`）全文件用一个换行符。一致的文件够用，不一致的文件就错了：
混合换行确实存在，而按多数换行符重写每一行会把**一行改动变成整篇 diff**。

插入用的换行符规则写死，不留"就近取主导"这种判断 —— 那句话至少有三种合理读法（取上一行、取下一行、
取前后十行多数），每种给同一个计划算出不同的预期哈希，而一个取决于实现者选了哪种读法的哈希
没法用来校验任何东西。

### 6.3 样书实测

```
=== 4. 无损渲染（样书真实的 75299 字节）===
   无损校验          原文可以无损读入并还原。
   行数              文件模型 2502  章节树模型 2503（差 1：旧模型把末尾空元素算作一行）
   默认换行符        LF
   逐行换行符是否一致 True
   渲染 69 个替换 → 行数 2502 → 2502（删除 0 / 替换 69 / 插入 0）
   实际变化的行数    69（应等于替换数）
   字节数            75299 → 75314
```

**两个模型对"行"的定义不同，这是查出来的、不是猜的**：旧模型把 `text.Split('\n')` 末尾那个空元素
当成一行（文件末尾本来就以换行结束），新模型按行终止符切分。两者对**任何真实存在的行**给出的行号
完全一致（69 个替换的位置逐一对上），差的只是末尾那个并不存在的"行"。

### 6.4 写测试时自己写错一次

我先前断言"样书末行没有换行符"。**断言失败暴露的是我的假设错，不是代码错** —— 末行有换行符。
那条断言已删除：2502 这个数字的正确性由无损往返本身保证，不需要额外的形态假设去背书。

---

## 7. 4d：TreePatch 与两模式等价性验证（已完成）

### 7.1 交付

| 文件 | 内容 |
|---|---|
| `TreePatch.cs` | `ChapterTreeMutation` 变化清单、`BodyAssignmentCalculator` 正文归属计算 |

`TreePatch` 的每条变化强制携带 `SourceBinding` 与 `ActionId`：

- **binding**：改动之后行号已经失去意义 —— 删过插过，第一个改动下面的全都移位了。变化只能说明
  自己指的是哪一类位置（原始行 / 插入操作 / 无物理来源），由坐标映射去解析。
- **ExpectedResult**：谁执行它谁就能证明建出来的树正是当初商定的那棵。没有这一件，恢复只能重跑
  一遍并祈祷。

### 7.2 Phase 4 真正的验收点：两种落地模式产出同一本书

```
TreeOnly：444 条，移除 160 行（只从成品移除，TXT 不动）
EditSource：160 个删除 → 补丁有效 → 渲染 2502 → 2342 行
=== 规范化模型对照 ===
   TreeOnly    444 章
   EditSource  444 章
   两者相等：True
```

"两种模式产出同一本书"在此之前只是一句设计意图，到这一步才被真的验证过。比的是章节顺序、标题、
层级、是否进目录，以及**每章拥有的正文内容** —— TreeOnly 用原文取文本，EditSource 用渲染后的
文本，行号各自按自己的版本算。

### 7.3 反证写错了，而这暴露了一件更有用的事

反证的作用是防止"两者相等"只是因为模型太粗、什么都判成相等。**第一版反证是错的**：

> 原本问："把被移除的行留着不删，模型会不会不同？" 实测**不会**。

查下来原因很清楚：那 160 行在**重建后的树里本来就不归任何章节**（实测仍被认领的为 0）。对以
章节归属为准的模型来说，它们存不存在根本不影响结果。**那个问题问错了，不是模型有问题。**

换成真正该问的两条，都通过：

1. 同一棵树配「原文」与配「完全不同的正文」→ 判成不等 ✅
2. 只改**一行**正文 → 也判成不等 ✅

同时把"被移除的行确实不在任何章节的正文里"固定成断言。

> **教训**：反证不是"随便找个东西改一改看会不会不等"。反证要针对检查真正claim的那件事。
> 我第一版针对的是"文件里还有没有这些字节"，而等价性检查 claim 的是"每章拥有什么正文" ——
> 两者根本不是一回事。写反证之前要先问：这个检查到底在断言什么？

---

## 8. 4f：Compile 纯函数（已完成，Phase 4 收尾）

### 8.1 交付

| 文件 | 内容 |
|---|---|
| `RepairPlanCompiler.cs` | `Compile(input)` → `TreePatch` + `SourcePatch` + 预期结果 |
| `RepairSourcePatchCompiler.cs` | `RepairEffect` → `SourceOperation` 的确定性投影 |
| `ChapterTreeEntry.StableKey` | 能跨重建存活的语义标识 |

### 8.2 样书实测

```
=== 1. 落地模式只决定用哪套产物 ===
   TreeOnly    可应用：568 条树变化，不动原文
   EditSource  可应用：568 条树变化，69 个文本操作
   两者都能应用    True
   树补丁完全相同  True  ← 模式不改变树要变成什么样
   预期结果相同    True

=== 2. 纯函数：同样输入给同样产物 ===
   两次编译操作序列相同  True
   两次编译树变化数相同  568 / 568
   书稿未被改动        True

=== 3. 产物构成 ===
   树变化 568 条：ChangeEntryLevel 429 / RetitleEntry 75 / RemoveEntry 38
                 / ReassignEntryBody 18 / InsertEntry 8
   文本操作 69 个：全部是 ReplaceOriginalLine
   移除的行 160

=== 4. 没勾任何一项 ===
   可执行 0 条树变化、0 个文本操作

=== 5. 若真的应用（只在内存渲染，不写盘）===
   2502 行 → 2502 行（替换 69，删除 0，插入 0）
   字节 75299 → 75314
   书稿仍然未变：True
```

**第 1 项是这一阶段最关键的一条**：两种落地模式下的树补丁**完全相同**。这就是"落地模式只决定用哪套产物，
从不决定一个动作是什么意思"这句话的实现 —— 在此之前它只是一句设计意图。

### 8.3 一个真实的设计缺口：`Entry.Id` 不能当稳定标识

第一版 `Compile` 有两个测试失败，根因是同一个：

```
Compiling_twice_from_the_same_input_gives_the_same_products   FAIL
The_tree_patch_is_the_same_in_both_modes                      FAIL
```

`BuildMutations` 用 `entry.Id` 在编译前后的两棵树之间匹配条目。而 `ReferencePlanner.Apply` 每次
重建都生成**新的 GUID**，于是同一个条目在两次编译之间被看成"既新增又移除"，产物自然对不上。

这不是测试写错了，而是**设计里缺了一件东西**：跨重建存活的语义标识。补上
`ChapterTreeEntry.StableKey`：

- 有标题行的章节 → `"L" + 行号`
- 没有标题行的（卷节点、前置内容、尚未插入标题的章节）→ `"T" + 标题`

行号加标题正是目录、定位器与重建后的树本来就一致认可的东西。补上之后两条测试都通过。

> 教训：凡是"比较两次计算结果"的设计，都要先问清**用什么比较**。用运行期生成的 id 去比较
> 两次运行的结果，是一个看起来能跑、但只在第一次跑得通的错。

### 8.4 测试

`RepairPlanCompilerTests.cs`，9 条：编译两次给同样产物、编译不碰书稿、TreeOnly 不建源补丁、
EditSource 两样都建、两种模式树补丁一致、默认选中在 TreeOnly 下不写任何东西、
改原文模式产出 69 个替换且不产出删除（用户没要求删）、没勾任何一项则产物为空、
决定顺序不影响产物。

**Core：592 → 601**（600 通过；`LongTextPerformanceTests` 是已知负载敏感项，单独跑通过）。

---

## 9. Phase 4 完成状态

| 子阶段 | 内容 | 状态 |
|---|---|---|
| 4a | `RepairEffect` 三维判别类型、`BodyAssignment`、`SourceAnchor` | ✅ |
| 4b | `CanonicalChapterModel`、`SourcePatch` + 八条规范化规则 | ✅ |
| 4c | `SourceTextDocument` 无损模型、`SourcePatchRenderer` | ✅ |
| 4d | `TreePatch`、`BodyAssignmentCalculator`、两模式等价性验证 | ✅ |
| 4e | `BodyAssignment` 接入动作描述（191 个动作全部可完整描述） | ✅ |
| 4f | `Compile` 纯函数、`RepairEffect` → `SourceOperation` 投影 | ✅ |

**Phase 4 的验收点已达成**：两种落地模式产出同一本书（等价性），且编译是纯函数、模式只决定用哪套产物。

Phase 4 通篇只做内存，一次也没有写过书稿 —— 第 2 项与第 5 项里的"书稿未被改动"就是证据。



